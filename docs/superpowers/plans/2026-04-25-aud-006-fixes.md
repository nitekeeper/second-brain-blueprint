# AUD-2026-04-25-006 Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix three WARNING findings from AUD-2026-04-25-006 across two files, then mark them RESOLVED in the audit report.

**Architecture:** Pure documentation edits — no code, no tests. Each task targets one finding (or one migration path within a finding). Verification uses grep to confirm old text is gone and new text is present. Tasks are independent after Task 1.

**Tech Stack:** Markdown, Edit tool, Grep tool, Bash (git)

---

## File Map

| File | Finding | Change |
|---|---|---|
| `user-guide.md` | AUD-027 | Update one table cell (line 228) |
| `template/scheduled-tasks/ops/migrate.md` | AUD-028 | Fix two approval blocks (hot.md disclosure) |
| `template/scheduled-tasks/ops/migrate.md` | AUD-029 | Add b2 backup step × 2 paths, update Confirm × 2, update Rollback section |
| `audits/AUD-2026-04-25-006.md` | All | Mark 027, 028, 029 RESOLVED |

---

## Task 1: AUD-027 — Fix Token Awareness table in `user-guide.md`

**Files:**
- Modify: `user-guide.md` (line 228)

- [ ] **Step 1: Verify the stale value is present**

```bash
grep "30,000–47,000" user-guide.md
```

Expected output:
```
| Audit all (full blueprint) | ~30,000–47,000 |
```

If this line is absent, the file has already been edited — stop and investigate.

- [ ] **Step 2: Apply the edit**

Using the Edit tool, replace:
```
| Audit all (full blueprint) | ~30,000–47,000 |
```
with:
```
| Audit all (full blueprint) | ~60,000–70,000 (CHANGELOG.md alone accounts for ~30,000+ tokens and grows with every audit cycle) |
```

- [ ] **Step 3: Verify the new value is present**

```bash
grep "60,000–70,000" user-guide.md
```

Expected output (one matching line):
```
| Audit all (full blueprint) | ~60,000–70,000 (CHANGELOG.md alone accounts for ~30,000+ tokens and grows with every audit cycle) |
```

- [ ] **Step 4: Commit**

```bash
git add user-guide.md
git commit -m "fix: update audit-all token estimate in user-guide.md (AUD-027)"
```

---

## Task 2: AUD-028 — Fix v2.2→v2.3 approval block in `migrate.md`

**Files:**
- Modify: `template/scheduled-tasks/ops/migrate.md` (v2.2→v2.3 approval block)

- [ ] **Step 1: Verify current (incorrect) state**

```bash
grep -A 12 "Migration: v2.2" template/scheduled-tasks/ops/migrate.md
```

Expected: `Files UNTOUCHED:` block contains `wiki/  (all pages, index, log, hot preserved)` and `Files UPDATED:` does NOT contain `wiki/hot.md`.

- [ ] **Step 2: Apply the edit**

Using the Edit tool, replace the entire UPDATED + UNTOUCHED block in the v2.2→v2.3 approval:

Old:
```
Files UPDATED:
  CLAUDE.md  (Query Routing Rule section replaced — behavior change, no size change)
  scheduled-tasks/ops/conventions.md  (Filing Answers step labels updated to v2.3 numbering)

Files UNTOUCHED:
  wiki/  (all pages, index, log, hot preserved)
```

New:
```
Files UPDATED:
  CLAUDE.md  (Query Routing Rule section replaced — behavior change, no size change)
  scheduled-tasks/ops/conventions.md  (Filing Answers step labels updated to v2.3 numbering)
  wiki/hot.md  (Schema: v2.2 → v2.3)

Files UNTOUCHED:
  wiki/  (all pages, index, log preserved)
```

- [ ] **Step 3: Verify**

```bash
grep -A 14 "Migration: v2.2" template/scheduled-tasks/ops/migrate.md
```

Expected: `Files UPDATED:` contains `wiki/hot.md  (Schema: v2.2 → v2.3)` and `Files UNTOUCHED:` reads `wiki/  (all pages, index, log preserved)` (no "hot").

---

## Task 3: AUD-028 — Fix v2.1→v2.3 approval block in `migrate.md`

**Files:**
- Modify: `template/scheduled-tasks/ops/migrate.md` (v2.1→v2.3 approval block)

- [ ] **Step 1: Verify current (incorrect) state**

```bash
grep -A 20 "Migration: v2.1" template/scheduled-tasks/ops/migrate.md
```

Expected: `Files UNTOUCHED:` block contains `wiki/  (all pages, index, log, hot preserved)` and `Files UPDATED:` does NOT contain `wiki/hot.md`.

- [ ] **Step 2: Apply the edit**

Using the Edit tool, replace the UPDATED + UNTOUCHED block in the v2.1→v2.3 approval:

Old:
```
Files UPDATED:
  scheduled-tasks/ops/audit.md       (recalibration sections removed)
  scheduled-tasks/ops/ingest.md      (bash → Python, post-op advisory added)
  scheduled-tasks/ops/lint.md        (bash → Python, post-op advisory added)
  scheduled-tasks/ops/conventions.md (updated for v2.3)

Files UNTOUCHED:
  wiki/  (all pages, index, log, hot preserved)
```

New:
```
Files UPDATED:
  scheduled-tasks/ops/audit.md       (recalibration sections removed)
  scheduled-tasks/ops/ingest.md      (bash → Python, post-op advisory added)
  scheduled-tasks/ops/lint.md        (bash → Python, post-op advisory added)
  scheduled-tasks/ops/conventions.md (updated for v2.3)
  wiki/hot.md  (Schema bumped to v2.3; Python: field added)

Files UNTOUCHED:
  wiki/  (all pages, index, log preserved)
```

- [ ] **Step 3: Verify**

```bash
grep -A 22 "Migration: v2.1" template/scheduled-tasks/ops/migrate.md
```

Expected: `Files UPDATED:` contains `wiki/hot.md  (Schema bumped to v2.3; Python: field added)` and `Files UNTOUCHED:` reads `wiki/  (all pages, index, log preserved)` (no "hot").

- [ ] **Step 4: Confirm "hot preserved" is gone from both blocks**

```bash
grep "hot preserved" template/scheduled-tasks/ops/migrate.md
```

Expected: no output (zero matches).

- [ ] **Step 5: Commit**

```bash
git add template/scheduled-tasks/ops/migrate.md
git commit -m "fix: disclose hot.md in UPDATED block for both migration paths (AUD-028)"
```

---

## Task 4: AUD-029 — Add conventions.md backup step to v2.2→v2.3 path

**Files:**
- Modify: `template/scheduled-tasks/ops/migrate.md` (v2.2→v2.3 execution + Confirm)

- [ ] **Step 1: Verify step b exists and b2 does not**

```bash
grep -n "backups/CLAUDE.md-v2.2" template/scheduled-tasks/ops/migrate.md
```

Expected: one result on the `b.` step line. No `b2` line adjacent.

- [ ] **Step 2: Add step b2 after step b**

Using the Edit tool, replace:

Old:
```
a. Create `backups/` directory if absent.
b. Copy `CLAUDE.md` → `backups/CLAUDE.md-v2.2-<YYYY-MM-DD>.bak`
c. In `CLAUDE.md`, replace the entire `## Query Routing Rule` section
```

New:
```
a. Create `backups/` directory if absent.
b. Copy `CLAUDE.md` → `backups/CLAUDE.md-v2.2-<YYYY-MM-DD>.bak`
b2. Copy `scheduled-tasks/ops/conventions.md` → `backups/conventions.md-v2.2-<YYYY-MM-DD>.bak`
c. In `CLAUDE.md`, replace the entire `## Query Routing Rule` section
```

- [ ] **Step 3: Update the v2.2→v2.3 Confirm message**

Using the Edit tool, replace:

Old:
```
> "Migration complete. Query routing is now wiki-first.
> Backup saved to `backups/CLAUDE.md-v2.2-<date>.bak` — delete when satisfied."
```

New:
```
> "Migration complete. Query routing is now wiki-first.
> Backups saved to `backups/CLAUDE.md-v2.2-<date>.bak` and `backups/conventions.md-v2.2-<date>.bak` — delete when satisfied."
```

- [ ] **Step 4: Verify b2 is present and Confirm message updated**

```bash
grep "conventions.md-v2.2" template/scheduled-tasks/ops/migrate.md
```

Expected: two matches — one for the `b2.` step, one in the Confirm message.

---

## Task 5: AUD-029 — Add conventions.md backup step to v2.1→v2.3 path

**Files:**
- Modify: `template/scheduled-tasks/ops/migrate.md` (v2.1→v2.3 execution + Confirm)

- [ ] **Step 1: Verify step b exists and b2 does not**

```bash
grep -n "backups/CLAUDE.md-v2.1" template/scheduled-tasks/ops/migrate.md
```

Expected: one result on the `b.` step line. No `b2` line adjacent.

- [ ] **Step 2: Add step b2 after step b**

Using the Edit tool, replace:

Old:
```
a. Create `backups/` directory if absent.
b. Copy `CLAUDE.md` → `backups/CLAUDE.md-v2.1-<YYYY-MM-DD>.bak`
c. Read `blueprint/template/CLAUDE.md`, substitute `[created-date]` and
```

New:
```
a. Create `backups/` directory if absent.
b. Copy `CLAUDE.md` → `backups/CLAUDE.md-v2.1-<YYYY-MM-DD>.bak`
b2. Copy `scheduled-tasks/ops/conventions.md` → `backups/conventions.md-v2.1-<YYYY-MM-DD>.bak`
c. Read `blueprint/template/CLAUDE.md`, substitute `[created-date]` and
```

- [ ] **Step 3: Update the v2.1→v2.3 Confirm message**

Using the Edit tool, replace:

Old:
```
> "Migration complete. Cold-start: ~7,780 → ~1,080 tokens. Query routing is now wiki-first.
> Backup saved to `backups/CLAUDE.md-v2.1-<date>.bak` — delete when satisfied."
```

New:
```
> "Migration complete. Cold-start: ~7,780 → ~1,080 tokens. Query routing is now wiki-first.
> Backups saved to `backups/CLAUDE.md-v2.1-<date>.bak` and `backups/conventions.md-v2.1-<date>.bak` — delete when satisfied."
```

- [ ] **Step 4: Verify b2 is present and Confirm message updated**

```bash
grep "conventions.md-v2.1" template/scheduled-tasks/ops/migrate.md
```

Expected: two matches — one for the `b2.` step, one in the Confirm message.

---

## Task 6: AUD-029 — Update Rollback section in `migrate.md`

**Files:**
- Modify: `template/scheduled-tasks/ops/migrate.md` (Rollback section)

- [ ] **Step 1: Verify current (incomplete) rollback bullets**

```bash
grep -A 3 "^## Rollback" template/scheduled-tasks/ops/migrate.md
```

Expected: both bullets mention only `CLAUDE.md` and `hot.md` — no reference to `conventions.md`.

- [ ] **Step 2: Apply the edit**

Using the Edit tool, replace both rollback bullets:

Old:
```
- **v2.2→v2.3:** Rename `backups/CLAUDE.md-v2.2-<date>.bak` → `CLAUDE.md`; revert `Schema:` in `hot.md` to `v2.2`.
- **v2.1→v2.3:** Rename `backups/CLAUDE.md-v2.1-<date>.bak` → `CLAUDE.md`; revert `Schema:` in `hot.md` to `v2.1`. All new ops files and scripts/ are inert without the v2.1 `CLAUDE.md` referencing them.
```

New:
```
- **v2.2→v2.3:** Rename `backups/CLAUDE.md-v2.2-<date>.bak` → `CLAUDE.md`; rename `backups/conventions.md-v2.2-<date>.bak` → `scheduled-tasks/ops/conventions.md`; revert `Schema:` in `hot.md` to `v2.2`.
- **v2.1→v2.3:** Rename `backups/CLAUDE.md-v2.1-<date>.bak` → `CLAUDE.md`; rename `backups/conventions.md-v2.1-<date>.bak` → `scheduled-tasks/ops/conventions.md`; revert `Schema:` in `hot.md` to `v2.1`. All new ops files and scripts/ are inert without the v2.1 `CLAUDE.md` referencing them.
```

- [ ] **Step 3: Verify rollback section is complete**

```bash
grep -A 3 "^## Rollback" template/scheduled-tasks/ops/migrate.md
```

Expected: both bullets now contain `conventions.md-v2.2` and `conventions.md-v2.1` respectively.

- [ ] **Step 4: Commit all AUD-029 changes**

```bash
git add template/scheduled-tasks/ops/migrate.md
git commit -m "fix: backup conventions.md during migration; update rollback procedures (AUD-029)"
```

---

## Task 7: Mark findings RESOLVED in audit report

**Files:**
- Modify: `audits/AUD-2026-04-25-006.md`

- [ ] **Step 1: Update finding status fields from OPEN to RESOLVED**

Three status rows to update (one per finding). Using the Edit tool, make three separate replacements.

For AUD-027 — replace:
```
| **ID** | AUD-2026-04-25-027 |
| **Severity** | WARNING |
| **Status** | OPEN |
```
with:
```
| **ID** | AUD-2026-04-25-027 |
| **Severity** | WARNING |
| **Status** | RESOLVED |
```

For AUD-028 — replace:
```
| **ID** | AUD-2026-04-25-028 |
| **Severity** | WARNING |
| **Status** | OPEN |
```
with:
```
| **ID** | AUD-2026-04-25-028 |
| **Severity** | WARNING |
| **Status** | RESOLVED |
```

For AUD-029 — replace:
```
| **ID** | AUD-2026-04-25-029 |
| **Severity** | WARNING |
| **Status** | OPEN |
```
with:
```
| **ID** | AUD-2026-04-25-029 |
| **Severity** | WARNING |
| **Status** | RESOLVED |
```

- [ ] **Step 2: Check off all three action items**

Using the Edit tool, replace:
```
- [ ] `AUD-2026-04-25-027` **[WARNING]** `user-guide.md` Token Awareness table shows stale `~30,000–47,000` for `!! audit all` — Owner: ___ | Target: ___
- [ ] `AUD-2026-04-25-028` **[WARNING]** `migrate.md` approval requests claim `hot.md` "preserved" in UNTOUCHED block but both migration paths patch it — Owner: ___ | Target: ___
- [ ] `AUD-2026-04-25-029` **[WARNING]** `migrate.md` rollback procedures don't revert `conventions.md`, silently reintroducing AUD-018's step-label mismatch — Owner: ___ | Target: ___
```
with:
```
- [x] `AUD-2026-04-25-027` **[WARNING]** `user-guide.md` Token Awareness table shows stale `~30,000–47,000` for `!! audit all` — Owner: ___ | Target: ___
- [x] `AUD-2026-04-25-028` **[WARNING]** `migrate.md` approval requests claim `hot.md` "preserved" in UNTOUCHED block but both migration paths patch it — Owner: ___ | Target: ___
- [x] `AUD-2026-04-25-029` **[WARNING]** `migrate.md` rollback procedures don't revert `conventions.md`, silently reintroducing AUD-018's step-label mismatch — Owner: ___ | Target: ___
```

- [ ] **Step 3: Verify no OPEN findings remain**

```bash
grep "| OPEN |" audits/AUD-2026-04-25-006.md
```

Expected: no output.

- [ ] **Step 4: Commit**

```bash
git add audits/AUD-2026-04-25-006.md
git commit -m "fix: apply AUD-2026-04-25-006 findings (027-029), mark all RESOLVED"
```
