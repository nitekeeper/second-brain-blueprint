# AUD-2026-04-25-010 Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Apply all 3 audit findings from AUD-2026-04-25-010, then mark all findings RESOLVED in the audit report.

**Architecture:** Pure documentation edits — no code, no tests. Three targeted text replacements across three markdown files, followed by an audit report status update, all landed in a single commit.

**Tech Stack:** Markdown, git

---

## File Map

| File | Task | Change type |
|---|---|---|
| `template/scheduled-tasks/ops/migrate.md` | Task 1 | Replace 6-line v2.1→v2.3 preamble with 1-sentence direct description |
| `template/scheduled-tasks/ops/audit.md` | Task 2 | Replace single-line token estimate with dual-mode bullet block |
| `template/scheduled-tasks/ops/blueprint-sync.md` | Task 3 | Update two cascade table rows |
| `audits/AUD-2026-04-25-010.md` | Task 4 | Set 3 statuses to RESOLVED; check 3 action items |

> **Note on TDD:** These are markdown documentation edits. There is no executable test suite. Verification for each task is a read-back check: after each edit, read the changed section and confirm the text matches the spec exactly.

---

### Task 1: Fix AUD-039 — migrate.md v2.1→v2.3 confusing preamble

**Files:**
- Modify: `template/scheduled-tasks/ops/migrate.md:125-131`

- [ ] **Step 1: Replace the preamble block**

In `template/scheduled-tasks/ops/migrate.md`, find the v2.1→v2.3 Path section. Locate the 6-line block that begins immediately after the `## v2.1 → v2.3 Path` heading:

```
Run all steps from the v2.1 → v2.2 migration (listed below), with two modifications:
- **Step 3i:** write `Schema: v2.3` (not `v2.2`) when patching `hot.md`
- **Step 3j:** append `## [YYYY-MM-DD] migrate | v2.1 → v2.3 — lean cold-start + wiki-first routing`

Because step 3c copies `CLAUDE.md` from `blueprint/template/CLAUDE.md`, which
already contains the v2.3 Query Routing Rule and v2.3 schema footer, the routing
patch and version bump are implicit.
```

Replace the entire block with:

```
Direct migration path from v2.1 to v2.3 in a single step — no intermediate v2.2 stop required. Step c copies `CLAUDE.md` from `blueprint/template/CLAUDE.md`, which already contains the v2.3 Query Routing Rule and v2.3 schema footer, so no separate routing patch is needed. The steps below are the complete v2.1→v2.3 procedure.
```

- [ ] **Step 2: Verify**

Read `template/scheduled-tasks/ops/migrate.md` lines 123–140. Confirm:
- The old phrase `Run all steps from the v2.1 → v2.2 migration (listed below)` is gone
- The old `Step 3i` and `Step 3j` bullets are gone
- The old `Because step 3c copies` rationale paragraph is gone
- The new single sentence `Direct migration path from v2.1 to v2.3 in a single step` is present
- The `### Pre-flight` heading still follows immediately after the new sentence
- No other lines in the file changed

---

### Task 2: Fix AUD-040 — audit.md token estimate unusable in blueprint-authoring mode

**Files:**
- Modify: `template/scheduled-tasks/ops/audit.md:96`

- [ ] **Step 1: Replace the token estimate bullet**

In `template/scheduled-tasks/ops/audit.md`, find the Notes section. Locate the bullet that begins:

```
- For `!! audit all`, expect ~35,000–45,000 tokens of reads for the tracked files. Run `python scripts/estimate_tokens.py blueprint/README.md blueprint/setup-guide.md blueprint/user-guide.md blueprint/troubleshooting.md blueprint/template/CLAUDE.md blueprint/template/scheduled-tasks/refresh-hot.md blueprint/template/scheduled-tasks/ops/*.md blueprint/skills/sqlite-query/*.md blueprint/skills/claude-code-enhanced/*.md blueprint/docs/audit-report-template.md` for a live estimate. Warn the user up front if the session is already close to context limits.
```

Replace it with:

```
- For `!! audit all`, expect ~35,000–45,000 tokens of reads for the tracked files.
  - **Deployed-user mode:** `python scripts/estimate_tokens.py blueprint/README.md blueprint/setup-guide.md blueprint/user-guide.md blueprint/troubleshooting.md blueprint/template/CLAUDE.md blueprint/template/scheduled-tasks/refresh-hot.md blueprint/template/scheduled-tasks/ops/*.md blueprint/skills/sqlite-query/*.md blueprint/skills/claude-code-enhanced/*.md blueprint/docs/audit-report-template.md`
  - **Blueprint-authoring mode** (`scripts/` absent): use `chars ÷ 4` as a manual estimate, or run `python template/scripts/estimate_tokens.py README.md setup-guide.md user-guide.md troubleshooting.md template/CLAUDE.md template/scheduled-tasks/refresh-hot.md template/scheduled-tasks/ops/*.md skills/sqlite-query/*.md skills/claude-code-enhanced/*.md docs/audit-report-template.md` from the repo root.

  Warn the user up front if the session is already close to context limits.
```

- [ ] **Step 2: Verify**

Read `template/scheduled-tasks/ops/audit.md` Notes section. Confirm:
- The old single-line `Run \`python scripts/estimate_tokens.py blueprint/...\` for a live estimate.` is gone
- The new bullet has a `**Deployed-user mode:**` sub-bullet with the `blueprint/` prefixed command
- The new bullet has a `**Blueprint-authoring mode**` sub-bullet with the `template/scripts/` path and repo-root paths
- `Warn the user up front if the session is already close to context limits.` is still present
- The `- For \`!! audit [Page Name]\`` bullet that follows is unchanged
- No other lines in the file changed

---

### Task 3: Fix AUD-041 — blueprint-sync.md cascade rows omit ops/reference.md

**Files:**
- Modify: `template/scheduled-tasks/ops/blueprint-sync.md:20-21`

- [ ] **Step 1: Update the "New scheduled task" cascade row**

In `template/scheduled-tasks/ops/blueprint-sync.md`, find the cascade table row:

```
| New scheduled task | `blueprint/template/scheduled-tasks/<name>.md` + `ops/audit.md` (informational parenthetical) + `ops/token-reference.md` (removed in v2.2; skip) + `setup-guide.md` + `README.md` and `user-guide.md` if user-visible + `template/CLAUDE.md` Directory Structure + `CHANGELOG.md` |
```

Replace it with:

```
| New scheduled task | `blueprint/template/scheduled-tasks/<name>.md` + `ops/audit.md` (informational parenthetical) + `setup-guide.md` + `README.md` and `user-guide.md` if user-visible + `ops/reference.md` (blueprint/ scheduled-tasks subtree) + `CHANGELOG.md` |
```

- [ ] **Step 2: Update the "New skill bundle added" cascade row**

Find the cascade table row:

```
| New skill bundle added | `blueprint/skills/<skill>/` + `blueprint/user-guide.md` + `blueprint/setup-guide.md` + `blueprint/ROADMAP.md` + `ops/conventions.md` if skill introduces a new hook contract |
```

Replace it with:

```
| New skill bundle added | `blueprint/skills/<skill>/` + `blueprint/user-guide.md` + `blueprint/setup-guide.md` + `blueprint/ROADMAP.md` + `ops/conventions.md` if skill introduces a new hook contract + `ops/reference.md` (blueprint/ skills subtree) |
```

- [ ] **Step 3: Verify**

Read `template/scheduled-tasks/ops/blueprint-sync.md`. Confirm:
- `ops/token-reference.md (removed in v2.2; skip)` is gone from the "New scheduled task" row
- `template/CLAUDE.md` Directory Structure is gone from the "New scheduled task" row
- `ops/reference.md (blueprint/ scheduled-tasks subtree)` is present in the "New scheduled task" row
- `ops/reference.md (blueprint/ skills subtree)` is present in the "New skill bundle added" row
- No other rows in the table changed

---

### Task 4: Update audit report — mark all findings RESOLVED

**Files:**
- Modify: `audits/AUD-2026-04-25-010.md`

- [ ] **Step 1: Set AUD-039 status to RESOLVED**

Find the AUD-039 finding table. Change:

```
| **Status** | OPEN |
```

to:

```
| **Status** | RESOLVED |
```

- [ ] **Step 2: Set AUD-040 status to RESOLVED**

Find the AUD-040 finding table. Change:

```
| **Status** | OPEN |
```

to:

```
| **Status** | RESOLVED |
```

- [ ] **Step 3: Set AUD-041 status to RESOLVED**

Find the AUD-041 finding table. Change:

```
| **Status** | OPEN |
```

to:

```
| **Status** | RESOLVED |
```

- [ ] **Step 4: Check off all Action Items**

In the Action Items section, change all three `- [ ]` entries to `- [x]`:

```
- [x] `AUD-2026-04-25-039` **[STYLE]** `migrate.md` v2.1→v2.3 preamble references non-existent section and uses vestigial step numbering — Owner: ___ | Target: ___
- [x] `AUD-2026-04-25-040` **[STYLE]** `audit.md` Notes token-estimate command is unusable in blueprint-authoring mode — Owner: ___ | Target: ___
- [x] `AUD-2026-04-25-041` **[STYLE]** `blueprint-sync.md` cascade rows omit `ops/reference.md` and reference a non-existent CLAUDE.md section — Owner: ___ | Target: ___
```

- [ ] **Step 5: Verify**

Read `audits/AUD-2026-04-25-010.md`. Confirm:
- All 3 finding tables show `| **Status** | RESOLVED |`
- All 3 Action Items entries show `- [x]`
- No other lines changed

---

### Task 5: Single commit

**Files:** all four modified files

- [ ] **Step 1: Stage all changed files**

```bash
git add template/scheduled-tasks/ops/migrate.md \
        template/scheduled-tasks/ops/audit.md \
        template/scheduled-tasks/ops/blueprint-sync.md \
        audits/AUD-2026-04-25-010.md
```

- [ ] **Step 2: Confirm staged files**

```bash
git diff --staged --stat
```

Expected output: 4 files changed. Verify no unexpected files are staged.

- [ ] **Step 3: Commit**

```bash
git commit -m "fix: resolve AUD-039 through AUD-041 from AUD-2026-04-25-010"
```

Expected: commit succeeds with 4 files changed.

- [ ] **Step 4: Verify**

```bash
git show --stat HEAD
```

Confirm the commit message and all 4 files appear in the output.
