# AUD-2026-04-26-002 Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Resolve AUD-050 and AUD-051 from audit AUD-2026-04-26-002 by applying 8 targeted text substitutions to `migrate.md` and marking both findings RESOLVED in the audit report.

**Architecture:** Single-pass edit of one source file (`template/scheduled-tasks/ops/migrate.md`) in document order — v2.2→v2.3 path edits first, v2.1→v2.3 path edits second — followed by an audit report update and a single commit.

**Tech Stack:** Plain text / Markdown editing only. No scripts, no tests, no build steps.

---

## File Map

| File | Action | Purpose |
|---|---|---|
| `template/scheduled-tasks/ops/migrate.md` | Modify | Apply 8 text substitutions (Edits 1–8) |
| `audits/AUD-2026-04-26-002.md` | Modify | Mark AUD-050 and AUD-051 RESOLVED |

---

### Task 1: Apply v2.2→v2.3 path edits (Edits 1–4)

**Files:**
- Modify: `template/scheduled-tasks/ops/migrate.md`

- [ ] **Step 1: Edit 1 — v2.2→v2.3 Approval request: add backup note to refresh-hot.md line**

In `template/scheduled-tasks/ops/migrate.md`, find this exact line in the v2.2→v2.3 Approval request block:

```
  scheduled-tasks/refresh-hot.md  (Step 3 updated to detect claude-code-enhanced skill)
```

Replace with:

```
  scheduled-tasks/refresh-hot.md  (Step 3 updated to detect claude-code-enhanced skill; backed up to backups/)
```

- [ ] **Step 2: Edit 2 — v2.2→v2.3 Backup steps: insert b4 before c**

Find this exact two-line sequence in the `On approval, execute in order` section of the v2.2→v2.3 path:

```
b3. Copy `scheduled-tasks/ops/blueprint-sync.md` → `backups/blueprint-sync.md-v2.2-<YYYY-MM-DD>.bak`
c. In `CLAUDE.md`, replace the entire `## Query Routing Rule` section
```

Replace with:

```
b3. Copy `scheduled-tasks/ops/blueprint-sync.md` → `backups/blueprint-sync.md-v2.2-<YYYY-MM-DD>.bak`
b4. Copy `scheduled-tasks/refresh-hot.md` → `backups/refresh-hot.md-v2.2-<YYYY-MM-DD>.bak`
c. In `CLAUDE.md`, replace the entire `## Query Routing Rule` section
```

- [ ] **Step 3: Edit 3 — v2.2→v2.3 Confirm: add refresh-hot.md backup to list**

Find this exact line in the v2.2→v2.3 Confirm block:

```
> Backups saved to `backups/CLAUDE.md-v2.2-<date>.bak`, `backups/conventions.md-v2.2-<date>.bak`, and `backups/blueprint-sync.md-v2.2-<date>.bak` — delete when satisfied.
```

Replace with:

```
> Backups saved to `backups/CLAUDE.md-v2.2-<date>.bak`, `backups/conventions.md-v2.2-<date>.bak`, `backups/blueprint-sync.md-v2.2-<date>.bak`, and `backups/refresh-hot.md-v2.2-<date>.bak` — delete when satisfied.
```

- [ ] **Step 4: Edit 4 — v2.2→v2.3 Rollback: insert refresh-hot.md restoration**

Find this exact fragment in the Rollback section, v2.2→v2.3 bullet:

```
rename `backups/blueprint-sync.md-v2.2-<date>.bak` → `scheduled-tasks/ops/blueprint-sync.md`; revert `Schema:` in `hot.md` to `v2.2`.
```

Replace with:

```
rename `backups/blueprint-sync.md-v2.2-<date>.bak` → `scheduled-tasks/ops/blueprint-sync.md`; rename `backups/refresh-hot.md-v2.2-<date>.bak` → `scheduled-tasks/refresh-hot.md`; revert `Schema:` in `hot.md` to `v2.2`.
```

- [ ] **Step 5: Verify v2.2→v2.3 edits**

Read lines 40–125 of `template/scheduled-tasks/ops/migrate.md` and confirm:
- The refresh-hot.md approval line ends with `; backed up to backups/)`
- `b4` appears between `b3` and `c`
- The confirm message lists four backup files ending with `refresh-hot.md-v2.2-<date>.bak`
- The rollback bullet includes `rename \`backups/refresh-hot.md-v2.2-<date>.bak\` → \`scheduled-tasks/refresh-hot.md\``

---

### Task 2: Apply v2.1→v2.3 path edits (Edits 5–8)

**Files:**
- Modify: `template/scheduled-tasks/ops/migrate.md`

- [ ] **Step 1: Edit 5 — v2.1→v2.3 Approval request: add backup note to refresh-hot.md line**

Find this exact line in the v2.1→v2.3 Approval request block (under `Files UPDATED:`):

```
  scheduled-tasks/refresh-hot.md  (Step 3 updated to detect claude-code-enhanced skill)
```

Replace with:

```
  scheduled-tasks/refresh-hot.md  (Step 3 updated to detect claude-code-enhanced skill; backed up to backups/)
```

Note: there are two identical lines in the file (one per migration path). Target the one inside the `## v2.1 → v2.3 Path` section.

- [ ] **Step 2: Edit 6 — v2.1→v2.3 Backup steps: insert g1.5 before g2**

Find this exact two-line sequence in the `On approval, execute in order` section of the v2.1→v2.3 path:

```
   `audit.md`, `ingest.md`, `lint.md`, `conventions.md`
g2. Copy `blueprint/template/scheduled-tasks/refresh-hot.md` → `scheduled-tasks/refresh-hot.md`
```

Replace with:

```
   `audit.md`, `ingest.md`, `lint.md`, `conventions.md`
g1.5. Copy `scheduled-tasks/refresh-hot.md` → `backups/refresh-hot.md-v2.1-<YYYY-MM-DD>.bak`
g2. Copy `blueprint/template/scheduled-tasks/refresh-hot.md` → `scheduled-tasks/refresh-hot.md`
```

- [ ] **Step 3: Edit 7 — v2.1→v2.3 Confirm: fix token figure + add refresh-hot.md backup**

Find this exact two-line block in the v2.1→v2.3 Confirm section:

```
> "Migration complete. Cold-start: ~7,780 → ~1,080 tokens. Query routing is now wiki-first.
> Backups saved to `backups/CLAUDE.md-v2.1-<date>.bak` and `backups/conventions.md-v2.1-<date>.bak` — delete when satisfied."
```

Replace with:

```
> "Migration complete. Cold-start: ~7,780 → ~2,100 tokens. Query routing is now wiki-first.
> Backups saved to `backups/CLAUDE.md-v2.1-<date>.bak`, `backups/conventions.md-v2.1-<date>.bak`, and `backups/refresh-hot.md-v2.1-<date>.bak` — delete when satisfied."
```

- [ ] **Step 4: Edit 8 — v2.1→v2.3 Rollback: insert refresh-hot.md restoration**

Find this exact fragment in the Rollback section, v2.1→v2.3 bullet:

```
rename `backups/conventions.md-v2.1-<date>.bak` → `scheduled-tasks/ops/conventions.md`; revert `Schema:` in `hot.md` to `v2.1`.
```

Replace with:

```
rename `backups/conventions.md-v2.1-<date>.bak` → `scheduled-tasks/ops/conventions.md`; rename `backups/refresh-hot.md-v2.1-<date>.bak` → `scheduled-tasks/refresh-hot.md`; revert `Schema:` in `hot.md` to `v2.1`.
```

- [ ] **Step 5: Verify v2.1→v2.3 edits**

Read lines 126–224 of `template/scheduled-tasks/ops/migrate.md` and confirm:
- The refresh-hot.md approval line (in the v2.1→v2.3 section) ends with `; backed up to backups/)`
- `g1.5` appears between `g` and `g2`
- The confirm message reads `~2,100 tokens` (not `~1,080`)
- The confirm message lists three backup files ending with `refresh-hot.md-v2.1-<date>.bak`
- The rollback bullet includes `rename \`backups/refresh-hot.md-v2.1-<date>.bak\` → \`scheduled-tasks/refresh-hot.md\``

---

### Task 3: Update audit report + commit

**Files:**
- Modify: `audits/AUD-2026-04-26-002.md`

- [ ] **Step 1: Mark AUD-050 RESOLVED in finding table**

In `audits/AUD-2026-04-26-002.md`, find the AUD-050 finding table row:

```
| **Status** | OPEN |
```

Replace with:

```
| **Status** | RESOLVED |
```

Target the table under `### [AUD-2026-04-26-050]`.

- [ ] **Step 2: Mark AUD-051 RESOLVED in finding table**

Find the AUD-051 finding table row:

```
| **Status** | OPEN |
```

Replace with:

```
| **Status** | RESOLVED |
```

Target the table under `### [AUD-2026-04-26-051]`.

- [ ] **Step 3: Check off both action items**

Find in the Action Items section:

```
- [ ] `AUD-2026-04-26-050` **[STYLE]** `migrate.md` v2.1→v2.3 confirm states stale "→ ~1,080 tokens" — Owner: ___ | Target: ___
- [ ] `AUD-2026-04-26-051` **[STYLE]** Both migration paths overwrite `refresh-hot.md` without backup; rollback omits restoration — Owner: ___ | Target: ___
```

Replace with:

```
- [x] `AUD-2026-04-26-050` **[STYLE]** `migrate.md` v2.1→v2.3 confirm states stale "→ ~1,080 tokens" — Owner: ___ | Target: ___
- [x] `AUD-2026-04-26-051` **[STYLE]** Both migration paths overwrite `refresh-hot.md` without backup; rollback omits restoration — Owner: ___ | Target: ___
```

- [ ] **Step 4: Update the summary table resolved count**

Find the summary table row:

```
| STYLE    | 2 | 0 | 2 | 0 |
```

Replace with:

```
| STYLE    | 2 | 0 | 0 | 2 |
```

Also update the Total row:

```
| **Total**| **2** | **0** | **2** | **0** |
```

Replace with:

```
| **Total**| **2** | **0** | **0** | **2** |
```

- [ ] **Step 5: Commit all changes**

```bash
git add template/scheduled-tasks/ops/migrate.md audits/AUD-2026-04-26-002.md
git commit -m "fix: resolve AUD-050 and AUD-051 from AUD-2026-04-26-002"
```

Expected: commit succeeds, `git status` shows clean working tree.
