# Audit AUD-2026-04-25-004 Fixes + documents/ Merge Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Apply all 8 open findings from AUD-2026-04-25-004 (cascade v2.3 wiki-first waterfall to every affected file), mark them RESOLVED, and merge `documents/` into `docs/`.

**Architecture:** Two commits — one for all audit content fixes + report update, one for the folder merge + reference cleanup. All changes are pure text edits or file moves; no code, no tests, no new dependencies.

**Tech Stack:** Git (file edits and `git mv`), markdown only.

---

## Files Touched

### Commit 1 — Audit fixes

| File | Change |
|---|---|
| `README.md` | Line 74: waterfall bullet text |
| `user-guide.md` | Lines 51–55: waterfall step list; ~line 187: compliance example |
| `template/scheduled-tasks/ops/conventions.md` | Line 125: "Filing Answers" heading + first sentence |
| `template/CLAUDE.md` | Step 2 bullet list: add third branch |
| `skills/sqlite-query/query-layer.md` | Line 5: step label |
| `template/scheduled-tasks/ops/audit.md` | Scope list: add CHANGELOG.md |
| `template/scheduled-tasks/ops/reference.md` | Blueprint template tree: add scripts/ subtree |
| `audits/AUD-2026-04-25-004.md` | Mark findings 015–022 RESOLVED |

### Commit 2 — Folder merge

| Action | Path |
|---|---|
| `git mv` | `documents/plans/2026-04-25-cold-start-optimization.md` → `docs/superpowers/plans/` |
| `git mv` | `documents/specifications/2026-04-25-cold-start-optimization-design.md` → `docs/superpowers/specs/` |
| `git mv` | `documents/audit-report-template.md` → `docs/` |
| Edit | `template/scheduled-tasks/ops/reference.md`: replace `documents/` with `docs/` in the working folder tree |
| Delete | `documents/` (empty after moves) |

---

### Task 1: Fix README.md — AUD-015 [CRITICAL]

**Files:**
- Modify: `README.md:74`

- [ ] **Step 1: Apply the fix**

In `README.md`, find and replace line 74 exactly:

Old:
```
- **Query waterfall** — answers from training first, then wiki, then web search
```

New:
```
- **Query waterfall** — wiki first, then web search, then training knowledge (fallback only, with confidence score)
```

- [ ] **Step 2: Verify**

Read `README.md` around line 74 and confirm the bullet now reads "wiki first."

---

### Task 2: Fix user-guide.md waterfall steps — AUD-016 [CRITICAL]

**Files:**
- Modify: `user-guide.md:51–56`

- [ ] **Step 1: Apply the fix**

In `user-guide.md`, find this exact block (lines 51–55):

```
1. **High confidence** → answers directly from training knowledge
2. **Wiki topic** → checks `index.md`, reads relevant pages, answers with citations
3. **Not in wiki** → searches the web, summarizes findings, asks if you want to ingest the source

After a Step 2 or Step 3 answer, the agent will ask: *"Worth filing this as an analysis page?"* Say yes to preserve the answer in your wiki permanently.
```

Replace it with:

```
1. **Wiki** → checks recent log, reads relevant pages via `index.md`, answers with `[[wiki link]]` citations
2. **Web search** → when wiki has nothing useful, or question needs current information; successful results auto-save to `wiki/inbox/`
3. **Training knowledge** *(fallback)* → when wiki and web both miss; answer includes `Confidence: N/10` staleness caveat

After a Step 1 (wiki synthesis) or Step 2 (web) answer, the agent will ask: *"Worth filing this as an analysis page?"* Say yes to preserve the answer in your wiki permanently.
```

- [ ] **Step 2: Verify**

Read `user-guide.md` lines 49–60 and confirm Step 1 is now "Wiki" and the filing-offer sentence references "Step 1 (wiki synthesis) or Step 2 (web)."

---

### Task 3: Fix user-guide.md compliance example — AUD-017 [WARNING]

**Files:**
- Modify: `user-guide.md:~187`

- [ ] **Step 1: Apply the fix**

In `user-guide.md`, find this exact sentence (in the Footer Commands section):

```
If you see `Step 1` on a wiki question, or a missing ops file read before an operation, the agent skipped a mandatory step and you can call it out immediately.
```

Replace it with:

```
If you see `Step 3` (training knowledge) on a question that should be in your wiki, or a missing ops file read before an operation, the agent skipped a mandatory step and you can call it out immediately.
```

- [ ] **Step 2: Verify**

Read `user-guide.md` around line 187 and confirm the sentence now references "Step 3 (training knowledge)."

---

### Task 4: Fix conventions.md Filing Answers section — AUD-018 [WARNING]

**Files:**
- Modify: `template/scheduled-tasks/ops/conventions.md:125–127`

- [ ] **Step 1: Apply the fix**

In `template/scheduled-tasks/ops/conventions.md`, find this exact block:

```
## Filing Answers (Query Step 2 / Step 3)

After any Step 2 (wiki) or Step 3 (web) answer, ask: "Worth filing this as an analysis page?"
```

Replace it with:

```
## Filing Answers (Query Step 1 / Step 2)

After any Step 1 (wiki synthesis) or Step 2 (web) answer, ask: "Worth filing this as an analysis page?"
```

- [ ] **Step 2: Verify**

Read `template/scheduled-tasks/ops/conventions.md` around line 125 and confirm the heading now reads "Step 1 / Step 2" and the body says "Step 1 (wiki synthesis) or Step 2 (web)."

---

### Task 5: Fix template/CLAUDE.md Step 2 missing branch — AUD-019 [WARNING]

**Files:**
- Modify: `template/CLAUDE.md` (Step 2 — Web Search section)

- [ ] **Step 1: Apply the fix**

In `template/CLAUDE.md`, under **Step 2 — Web Search**, find this exact block:

```
- If the result **directly answered the question** → silently save to `wiki/inbox/` and read `@scheduled-tasks/ops/ingest.md` to ingest it
- If the result is **loosely related but did not answer** → skip ingest; use partial findings to inform Step 3
```

Replace it with:

```
- If the result **directly answered the question** → silently save to `wiki/inbox/` and read `@scheduled-tasks/ops/ingest.md` to ingest it
- If the result **directly answered the question AND Step 1 returned partial wiki content** → ingest the web result; answer citing both the wiki pages and the web result
- If the result is **loosely related but did not answer** → skip ingest; use partial findings to inform Step 3
```

- [ ] **Step 2: Verify**

Read `template/CLAUDE.md` Step 2 section and confirm three bullets are present, with the new combined-source branch in the middle.

---

### Task 6: Fix query-layer.md step label — AUD-020 [WARNING]

**Files:**
- Modify: `skills/sqlite-query/query-layer.md:5`

- [ ] **Step 1: Apply the fix**

In `skills/sqlite-query/query-layer.md`, find this exact sentence on line 5:

```
It replaces sub-steps 2–3 of the wiki-check branch (Step 2) in the Query Routing Rule waterfall.
```

Replace it with:

```
It replaces sub-steps 2–3 of Step 1 (wiki) in the Query Routing Rule waterfall.
```

- [ ] **Step 2: Verify**

Read `skills/sqlite-query/query-layer.md` line 5 and confirm it now reads "Step 1 (wiki)."

---

### Task 7: Fix audit.md scope list — AUD-021 [STYLE]

**Files:**
- Modify: `template/scheduled-tasks/ops/audit.md:15–20`

- [ ] **Step 1: Apply the fix**

In `template/scheduled-tasks/ops/audit.md`, in the `!! audit all` scope list, find:

```
- `blueprint/troubleshooting.md`
- `blueprint/LICENSE`
```

Replace it with:

```
- `blueprint/troubleshooting.md`
- `blueprint/CHANGELOG.md`
- `blueprint/LICENSE`
```

- [ ] **Step 2: Verify**

Read `template/scheduled-tasks/ops/audit.md` lines 14–25 and confirm `blueprint/CHANGELOG.md` appears between `troubleshooting.md` and `LICENSE`.

---

### Task 8: Fix reference.md blueprint template tree — AUD-022 [STYLE]

**Files:**
- Modify: `template/scheduled-tasks/ops/reference.md:36–38`

- [ ] **Step 1: Apply the fix**

In `template/scheduled-tasks/ops/reference.md`, find this exact block in the directory tree:

```
│   ├── template/
│   │   ├── CLAUDE.md
│   │   └── scheduled-tasks/
```

Replace it with:

```
│   ├── template/
│   │   ├── CLAUDE.md
│   │   ├── scripts/
│   │   │   ├── check_deps.py
│   │   │   ├── estimate_tokens.py
│   │   │   ├── file_check.py
│   │   │   ├── log_tail.py
│   │   │   ├── ready.py
│   │   │   └── wrap.py
│   │   └── scheduled-tasks/
```

- [ ] **Step 2: Verify**

Read `template/scheduled-tasks/ops/reference.md` lines 34–52 and confirm the `scripts/` subtree with all 6 files appears between `CLAUDE.md` and `scheduled-tasks/`.

---

### Task 9: Mark findings RESOLVED in audit report + Commit 1

**Files:**
- Modify: `audits/AUD-2026-04-25-004.md`

- [ ] **Step 1: Mark all 8 findings RESOLVED**

In `audits/AUD-2026-04-25-004.md`, for each of the 8 findings (AUD-2026-04-25-015 through AUD-2026-04-25-022), find the Status row in its table:

```
| **Status** | OPEN |
```

Replace with:

```
| **Status** | RESOLVED |
```

Repeat for all 8 findings.

- [ ] **Step 2: Update the executive summary counts**

Find the severity summary table:

```
| CRITICAL | 2 | 0 | 2 | 0 |
| WARNING  | 4 | 0 | 4 | 0 |
| STYLE    | 2 | 0 | 2 | 0 |
| **Total**| **8** | **0** | **8** | **0** |
```

Replace with (columns are: Severity | New This Cycle | Carried Over | Total Open | Resolved This Cycle):

```
| CRITICAL | 2 | 0 | 0 | 2 |
| WARNING  | 4 | 0 | 0 | 4 |
| STYLE    | 2 | 0 | 0 | 2 |
| **Total**| **8** | **0** | **0** | **8** |
```

- [ ] **Step 3: Update Action Items checkboxes**

In the Action Items section, change each `- [ ]` to `- [x]` for all 8 items.

- [ ] **Step 4: Commit**

```bash
git add README.md user-guide.md \
  template/scheduled-tasks/ops/conventions.md \
  template/CLAUDE.md \
  skills/sqlite-query/query-layer.md \
  template/scheduled-tasks/ops/audit.md \
  template/scheduled-tasks/ops/reference.md \
  audits/AUD-2026-04-25-004.md
git commit -m "fix: apply AUD-2026-04-25-004 findings (015-022), mark all RESOLVED"
```

---

### Task 10: Move documents/ files into docs/ — Folder Merge

**Files:**
- Move: `documents/plans/2026-04-25-cold-start-optimization.md` → `docs/superpowers/plans/`
- Move: `documents/specifications/2026-04-25-cold-start-optimization-design.md` → `docs/superpowers/specs/`
- Move: `documents/audit-report-template.md` → `docs/`

- [ ] **Step 1: Move the plan file**

```bash
git mv "documents/plans/2026-04-25-cold-start-optimization.md" \
       "docs/superpowers/plans/2026-04-25-cold-start-optimization.md"
```

- [ ] **Step 2: Move the spec file**

```bash
git mv "documents/specifications/2026-04-25-cold-start-optimization-design.md" \
       "docs/superpowers/specs/2026-04-25-cold-start-optimization-design.md"
```

- [ ] **Step 3: Move the audit report template**

```bash
git mv "documents/audit-report-template.md" "docs/audit-report-template.md"
```

- [ ] **Step 4: Verify all three moves are staged**

```bash
git status
```

Expected: three renames staged (R  documents/... → docs/...), `documents/plans/` and `documents/specifications/` directories now empty.

---

### Task 11: Update reference.md working folder tree + delete documents/

**Files:**
- Modify: `template/scheduled-tasks/ops/reference.md:17`
- Delete: `documents/` (empty directories)

- [ ] **Step 1: Update the working folder tree in reference.md**

In `template/scheduled-tasks/ops/reference.md`, find this line in the `<WorkingFolder>/` tree:

```
├── documents/                  ← Audit report template and design specifications. Developer use only.
```

Replace it with:

```
├── docs/                       ← Design specs and implementation plans. Developer use only.
```

- [ ] **Step 2: Remove the now-empty documents/ directories**

```bash
rmdir "documents/plans" "documents/specifications" "documents"
```

(Windows: these are empty after the `git mv` steps above, so `rmdir` with no flags works.)

- [ ] **Step 3: Verify documents/ is gone**

```bash
git status
```

Expected: `documents/plans/` and `documents/specifications/` deleted (git already tracked the moves); `template/scheduled-tasks/ops/reference.md` shows as modified.

- [ ] **Step 4: Commit**

```bash
git add template/scheduled-tasks/ops/reference.md
git commit -m "refactor: merge documents/ into docs/, remove documents/"
```
