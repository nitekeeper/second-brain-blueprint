# v2.3 Schema Version Bump Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bump schema to v2.3, update `!! migrate` with version-aware branching for v2.1→v2.3 and v2.2→v2.3, and record the release in CHANGELOG.

**Architecture:** Three independent file edits — `template/CLAUDE.md` (2 line changes), `template/scheduled-tasks/ops/migrate.md` (full rewrite with branching), `CHANGELOG.md` (prepend entry). No code, no tests — verification is reading the edited content back.

**Tech Stack:** Markdown, git

---

### Task 1: Update template/CLAUDE.md — schema version + detection

**Files:**
- Modify: `template/CLAUDE.md:16` (startup detection)
- Modify: `template/CLAUDE.md:142` (schema footer)

- [ ] **Step 1: Verify current content**

Read `template/CLAUDE.md` lines 15–17. Confirm line 16 reads:

    - If `hot.md`'s `Schema:` is below `v2.2`: announce "Blueprint v2.2 is available — run `!! migrate` to update." (once per session)

Read lines 141–143. Confirm line 142 reads:

    *Schema version: 2.2 | Created: [created-date] | Updated: [updated-date]*

- [ ] **Step 2: Update startup detection (line 16)**

Replace the line containing `below v2.2` with:

    - If `hot.md`'s `Schema:` is below `v2.3`: announce "Blueprint v2.3 is available — run `!! migrate` to update." (once per session)

- [ ] **Step 3: Update schema footer (line 142)**

Replace:

    *Schema version: 2.2 | Created: [created-date] | Updated: [updated-date]*

With:

    *Schema version: 2.3 | Created: [created-date] | Updated: [updated-date]*

- [ ] **Step 4: Verify both changes**

Read lines 15–17 and 141–143 again. Confirm:
- Line 16 contains `below v2.3` and `Blueprint v2.3 is available`
- Line 142 contains `Schema version: 2.3`

- [ ] **Step 5: Commit**

```bash
git add template/CLAUDE.md
git commit -m "feat: bump schema version to v2.3 in CLAUDE.md"
```

Expected: `1 file changed`

---

### Task 2: Rewrite template/scheduled-tasks/ops/migrate.md — version-aware branching

**Files:**
- Modify: `template/scheduled-tasks/ops/migrate.md` (full rewrite)

- [ ] **Step 1: Verify current content**

Read `template/scheduled-tasks/ops/migrate.md`. Confirm line 5 reads:
`Upgrades an existing v2.1.x working folder to v2.2.`

- [ ] **Step 2: Write the file header and detection section**

Overwrite `template/scheduled-tasks/ops/migrate.md` with this opening (more sections follow in steps 3–6 — write them all before saving, or append each in sequence):

**Section A — Header and detection:**

```
# Op: MIGRATE

Triggered when the user says `!! migrate`.

Upgrades an existing working folder to v2.3.

---

## Detection (runs at startup)

During startup step 7, compare `Schema:` in `hot.md` against the current schema
version (`v2.3`). If `Schema:` is below `v2.3`, announce once:

> "Blueprint v2.3 is available — run `!! migrate` to update your working folder."

Announce at most once per session (do not repeat on every response).

---

## Steps

1. **Read `Schema:` from `hot.md`** to determine migration path:
   - `v2.2` → follow the **v2.2 → v2.3** path below
   - `v2.1` or below → follow the **v2.1 → v2.3** path below
   - `v2.3` → announce "Already on v2.3. Nothing to migrate." and stop.

---
```

- [ ] **Step 3: Append the v2.2 → v2.3 path**

Append the following to the file:

**Section B — v2.2 → v2.3 path:**

```
## v2.2 → v2.3 Path

### Pre-flight

Run `python scripts/check_deps.py --python`
- If exit non-zero: stop. Show the printed instructions. Do not continue.

### Approval request

Show the user:

    Migration: v2.2 → v2.3

    Files UPDATED:
      CLAUDE.md  (Query Routing Rule section replaced — behavior change, no size change)

    Files UNTOUCHED:
      wiki/  (all pages, index, log, hot preserved)
      memory.md, raw/, drafts/
      scheduled-tasks/ops/  (all ops files preserved)
      scripts/  (all scripts preserved)
      sqlite-query skill (if installed)

    Shall I proceed?

### On approval, execute in order

a. Create `backups/` directory if absent.
b. Copy `CLAUDE.md` → `backups/CLAUDE.md-v2.2-<YYYY-MM-DD>.bak`
c. In `CLAUDE.md`, replace the entire `## Query Routing Rule` section (from the
   `## Query Routing Rule` heading through and including its closing `---` separator,
   up to but not including `## Ops Routing`) with the following exact text:

    ## Query Routing Rule

    **CRITICAL: Follow this waterfall for every user question — no exception for perceived simplicity or confidence level.**

    **Step 1 — Wiki** *(always first, no conditions)*
    1. Run `python scripts/log_tail.py` for last 5 log entries
    2. If `scheduled-tasks/query-layer.md` exists → read and follow it; fall back to step 3 on empty/failure
    3. Grep `wiki/pages` for topic slug; if no match, read `wiki/index.md`
    4. Read candidate pages; answer with `[[wiki link]]` citations
    5. Ask: "Worth filing as an analysis page?" — if yes, read `@scheduled-tasks/ops/conventions.md` first

    If wiki answers the question → stop here.

    **Step 2 — Web Search**
    Runs when: (a) wiki returned nothing useful, OR (b) question needs current or recent information.
    - Search and summarize
    - If the result **directly answered the question** → silently save to `wiki/inbox/` and read `@scheduled-tasks/ops/ingest.md` to ingest it
    - If the result is **loosely related but did not answer** → skip ingest; use partial findings to inform Step 3

    **Step 3 — Training Knowledge** *(fallback only)*
    Used when wiki and web both miss or are unavailable. Always append:
    `Confidence: N/10 — [one-line caveat if score ≤ 7 or topic is time-sensitive]`

    Omit the caveat when score is 8–10 and the topic is not time-sensitive.

    **Edge cases:**
    - Blueprint-authoring mode (no `wiki/` at root): Skip Step 1; go straight to Step 2 → Step 3
    - Web search unavailable: Skip Step 2; fall to Step 3 with note: *"Web search unavailable."*

    ---

d. In `CLAUDE.md`, also update the startup detection line and schema footer:
   - Line containing `below v2.2`: change to `below v2.3` and `Blueprint v2.3`
   - Footer line `Schema version: 2.2`: change to `Schema version: 2.3`
   *(Skip step d if Task 1 of this plan was already applied — check the footer first.)*
e. Patch `hot.md`: update `Schema: v2.2` → `Schema: v2.3`
f. Append to `wiki/log.md`:
   `## [YYYY-MM-DD] migrate | v2.2 → v2.3 — wiki-first query routing redesign`
   (≤500 chars)

### Confirm

> "Migration complete. Query routing is now wiki-first.
> Backup saved to `backups/CLAUDE.md-v2.2-<date>.bak` — delete when satisfied."

---
```

- [ ] **Step 4: Append the v2.1 → v2.3 path**

Append the following to the file:

**Section C — v2.1 → v2.3 path:**

```
## v2.1 → v2.3 Path

Run all steps from the v2.1 → v2.2 migration (listed below), with two modifications:
- **Step 3i:** write `Schema: v2.3` (not `v2.2`) when patching `hot.md`
- **Step 3j:** append `## [YYYY-MM-DD] migrate | v2.1 → v2.3 — lean cold-start + wiki-first routing`

Because step 3c copies `CLAUDE.md` from `blueprint/template/CLAUDE.md`, which
already contains the v2.3 Query Routing Rule and v2.3 schema footer, the routing
patch and version bump are implicit.

### Pre-flight

Run `python scripts/check_deps.py --python`
- If exit non-zero: stop. Show the printed instructions. Do not continue.

### Approval request

Show the user:

    Migration: v2.1 → v2.3

    Files REPLACED:
      CLAUDE.md  (rewritten — ~86% smaller, wiki-first query routing)

    Files ADDED:
      scheduled-tasks/ops/session-memory.md
      scheduled-tasks/ops/blueprint-sync.md
      scheduled-tasks/ops/reference.md
      scheduled-tasks/ops/session-hygiene.md
      scheduled-tasks/ops/migrate.md
      scripts/check_deps.py
      scripts/wrap.py
      scripts/ready.py
      scripts/log_tail.py
      scripts/file_check.py
      scripts/estimate_tokens.py

    Files DELETED:
      scheduled-tasks/ops/token-reference.md

    Files UPDATED:
      scheduled-tasks/ops/audit.md       (recalibration sections removed)
      scheduled-tasks/ops/ingest.md      (bash → Python, post-op advisory added)
      scheduled-tasks/ops/lint.md        (bash → Python, post-op advisory added)
      scheduled-tasks/ops/conventions.md (updated for v2.3)

    Files UNTOUCHED:
      wiki/  (all pages, index, log, hot preserved)
      memory.md, raw/, drafts/
      sqlite-query skill (if installed)

    Shall I proceed?

### On approval, execute in order

a. Create `backups/` directory if absent.
b. Copy `CLAUDE.md` → `backups/CLAUDE.md-v2.1-<YYYY-MM-DD>.bak`
c. Read `blueprint/template/CLAUDE.md`, substitute `[created-date]` and
   `[updated-date]` with today's date, remove the `> **Setup note:**` block,
   write result to `CLAUDE.md`.
d. Copy each new ops file from `blueprint/template/scheduled-tasks/ops/` to
   `scheduled-tasks/ops/`:
   `session-memory.md`, `blueprint-sync.md`, `reference.md`,
   `session-hygiene.md`, `migrate.md`
e. Create `scripts/` directory if absent.
f. Copy each script from `blueprint/template/scripts/` to `scripts/`:
   `check_deps.py`, `wrap.py`, `ready.py`, `log_tail.py`,
   `file_check.py`, `estimate_tokens.py`
g. Copy updated ops files from `blueprint/template/scheduled-tasks/ops/` to
   `scheduled-tasks/ops/`:
   `audit.md`, `ingest.md`, `lint.md`, `conventions.md`
h. Delete `scheduled-tasks/ops/token-reference.md` if it exists.
i. Patch `hot.md`:
   - Add `Python: [python | python3]` line (detect by running
     `python --version` or `python3 --version` and using whichever succeeds)
   - Update `Schema: v2.1` → `Schema: v2.3`
j. Append to `wiki/log.md`:
   `## [YYYY-MM-DD] migrate | v2.1 → v2.3 — lean cold-start + wiki-first routing`
   (≤500 chars)

### Confirm

> "Migration complete. Cold-start: ~7,780 → ~1,080 tokens. Query routing is now wiki-first.
> Backup saved to `backups/CLAUDE.md-v2.1-<date>.bak` — delete when satisfied."

---
```

- [ ] **Step 5: Append the skill offer and rollback sections**

Append the following to the file:

**Section D — Skill offer and rollback:**

```
## Offer Claude Code enhanced skill (both paths)

> "Are you using Claude Code CLI? Run `!! install claude-code-enhanced` to add
> /wrap, /ready, and /migrate as native slash commands."

---

## Rollback

- **v2.2→v2.3:** Rename `backups/CLAUDE.md-v2.2-<date>.bak` → `CLAUDE.md`; revert `Schema:` in `hot.md` to `v2.2`.
- **v2.1→v2.3:** Rename `backups/CLAUDE.md-v2.1-<date>.bak` → `CLAUDE.md`; revert `Schema:` in `hot.md` to `v2.1`. All new ops files and scripts/ are inert without the v2.1 `CLAUDE.md` referencing them.
```

- [ ] **Step 6: Verify the complete file**

Read `template/scheduled-tasks/ops/migrate.md`. Confirm:
- Line 5: "Upgrades an existing working folder to v2.3."
- Detection section references `v2.3`
- Three branches present: v2.2→v2.3, v2.1→v2.3, already on v2.3
- v2.2→v2.3 path step c contains the full Query Routing Rule replacement text (Step 1 Wiki, Step 2 Web Search, Step 3 Training Knowledge, Edge cases)
- v2.1→v2.3 path step i sets `Schema: v2.3`
- Rollback section present for both paths

- [ ] **Step 7: Commit**

```bash
git add template/scheduled-tasks/ops/migrate.md
git commit -m "feat: update migrate.md — version-aware v2.1→v2.3 and v2.2→v2.3 paths"
```

Expected: `1 file changed`

---

### Task 3: Update CHANGELOG.md — add v2.3.0 entry

**Files:**
- Modify: `CHANGELOG.md`

- [ ] **Step 1: Verify current top entry**

Read `CHANGELOG.md` lines 1–10. Confirm the first version entry is `## v2.2.0 — 2026-04-25`.

- [ ] **Step 2: Prepend v2.3.0 entry**

Insert the following block after the `> Version history...` blockquote and before `## v2.2.0`:

```
## v2.3.0 — 2026-04-25

### Query Routing Rule redesign

**Wiki-first waterfall:** Agent no longer leads with training knowledge.
Routing is now deterministic — no judgment call at the top of the chain:

- Step 1 — Wiki (always first, no conditions)
- Step 2 — Web Search (when wiki misses or question needs current info);
  successful results auto-ingest to `wiki/inbox/` with no user prompt
- Step 3 — Training Knowledge (fallback only); always reports
  `Confidence: N/10` with staleness caveat when score ≤ 7

**`!! migrate` updated:** Handles v2.1 → v2.3 and v2.2 → v2.3 in a single
command. v2.2 users get a lightweight patch (one section swap + Schema bump).

```

- [ ] **Step 3: Verify**

Read `CHANGELOG.md` lines 1–20. Confirm `## v2.3.0 — 2026-04-25` appears before `## v2.2.0 — 2026-04-25`.

- [ ] **Step 4: Commit**

```bash
git add CHANGELOG.md
git commit -m "docs: add v2.3.0 changelog entry"
```

Expected: `1 file changed`
