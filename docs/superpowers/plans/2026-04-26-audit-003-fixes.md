# AUD-2026-04-26-003 Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Resolve AUD-052, AUD-053, and AUD-054 from audit report AUD-2026-04-26-003 in a single commit.

**Architecture:** Three markdown text edits (no code changes) — two substitutions in `session-hygiene.md`, one parenthetical extension in `audit.md`, and three status+action-item updates in the audit report. All changes are in-place substitutions with no structural impact on any file.

**Tech Stack:** Markdown only. No scripts, no dependencies.

---

## File Map

| Action | File | What changes |
|---|---|---|
| Modify | `template/scheduled-tasks/ops/session-hygiene.md` | 2× `!![op]` → `!! [op]` in template blocks |
| Modify | `template/scheduled-tasks/ops/audit.md` | Step 0 parenthetical extended with blueprint-authoring mode fallback |
| Modify | `audits/AUD-2026-04-26-003.md` | 3× Status `OPEN` → `RESOLVED`; 3× action item `- [ ]` → `- [x]` |

---

## Task 1: Fix `session-hygiene.md` — AUD-053

**Files:**
- Modify: `template/scheduled-tasks/ops/session-hygiene.md`

- [ ] **Step 1: Verify the current state of both template blocks**

  Read the file and confirm both occurrences of `!![op]` are present before editing:

  ```
  Expected (Intercept message block, ~line 13):
    ⚠️  A !![op] operation completed earlier in this session.

  Expected (Post-op advisory block, ~line 33):
    ⚠️  Session advisory: This session has completed a !![op] operation and the
  ```

- [ ] **Step 2: Edit — Intercept message block**

  In `template/scheduled-tasks/ops/session-hygiene.md`, replace in the Intercept message block:

  ```
  Old: ⚠️  A !![op] operation completed earlier in this session.
  New: ⚠️  A !! [op] operation completed earlier in this session.
  ```

- [ ] **Step 3: Edit — Post-op advisory block**

  In `template/scheduled-tasks/ops/session-hygiene.md`, replace in the Post-op advisory block:

  ```
  Old: ⚠️  Session advisory: This session has completed a !![op] operation and the
  New: ⚠️  Session advisory: This session has completed a !! [op] operation and the
  ```

- [ ] **Step 4: Verify both substitutions**

  Re-read `template/scheduled-tasks/ops/session-hygiene.md` and confirm:
  - `!! [op]` appears in the Intercept message block
  - `!! [op]` appears in the Post-op advisory block
  - `!![op]` (no space) appears nowhere in the file

---

## Task 2: Fix `audit.md` — AUD-054

**Files:**
- Modify: `template/scheduled-tasks/ops/audit.md`

- [ ] **Step 1: Verify the current state of step 0**

  Read `template/scheduled-tasks/ops/audit.md` and locate the step 0 sentence. Confirm it currently reads:

  ```
  Check whether an `audits/` directory exists (`python scripts/file_check.py audits/`).
  ```

- [ ] **Step 2: Edit — Extend step 0 parenthetical**

  In `template/scheduled-tasks/ops/audit.md`, replace in step 0:

  ```
  Old:
  Check whether an `audits/` directory exists (`python scripts/file_check.py audits/`).

  New:
  Check whether an `audits/` directory exists (`python scripts/file_check.py audits/`; blueprint-authoring mode: `python template/scripts/file_check.py audits/` or `ls audits/`).
  ```

- [ ] **Step 3: Verify the edit**

  Re-read `template/scheduled-tasks/ops/audit.md` step 0 and confirm:
  - The sentence ends with `...or \`ls audits/\`).`
  - The rest of the step 0 paragraph is unchanged
  - No other lines in the file changed

---

## Task 3: Update audit report — AUD-052, AUD-053, AUD-054

**Files:**
- Modify: `audits/AUD-2026-04-26-003.md`

- [ ] **Step 1: Resolve AUD-052 — set Status to RESOLVED**

  In `audits/AUD-2026-04-26-003.md`, in the AUD-2026-04-26-052 finding table:

  ```
  Old: | **Status** | OPEN |
  New: | **Status** | RESOLVED |
  ```

- [ ] **Step 2: Resolve AUD-053 — set Status to RESOLVED**

  In `audits/AUD-2026-04-26-003.md`, in the AUD-2026-04-26-053 finding table:

  ```
  Old: | **Status** | OPEN |
  New: | **Status** | RESOLVED |
  ```

- [ ] **Step 3: Resolve AUD-054 — set Status to RESOLVED**

  In `audits/AUD-2026-04-26-003.md`, in the AUD-2026-04-26-054 finding table:

  ```
  Old: | **Status** | OPEN |
  New: | **Status** | RESOLVED |
  ```

- [ ] **Step 4: Check off AUD-052 action item**

  In the Action Items section:

  ```
  Old: - [ ] `AUD-2026-04-26-052` **[WARNING]** `ROADMAP.md` referenced in `reference.md` tree and `blueprint-sync.md` cascade but file does not exist — Owner: ___ | Target: ___
  New: - [x] `AUD-2026-04-26-052` **[WARNING]** `ROADMAP.md` referenced in `reference.md` tree and `blueprint-sync.md` cascade but file does not exist — Owner: ___ | Target: ___
  ```

- [ ] **Step 5: Check off AUD-053 action item**

  In the Action Items section:

  ```
  Old: - [ ] `AUD-2026-04-26-053` **[STYLE]** `session-hygiene.md` `!![op]` placeholder produces missing-space format — Owner: ___ | Target: ___
  New: - [x] `AUD-2026-04-26-053` **[STYLE]** `session-hygiene.md` `!![op]` placeholder produces missing-space format — Owner: ___ | Target: ___
  ```

- [ ] **Step 6: Check off AUD-054 action item**

  In the Action Items section:

  ```
  Old: - [ ] `AUD-2026-04-26-054` **[STYLE]** `audit.md` step 0 file-check has no blueprint-authoring mode fallback — Owner: ___ | Target: ___
  New: - [x] `AUD-2026-04-26-054` **[STYLE]** `audit.md` step 0 file-check has no blueprint-authoring mode fallback — Owner: ___ | Target: ___
  ```

- [ ] **Step 7: Verify audit report**

  Re-read `audits/AUD-2026-04-26-003.md` and confirm:
  - All three finding tables show `| **Status** | RESOLVED |`
  - All three action items show `- [x]`
  - No other content in the report was changed

---

## Task 4: Commit

**Files:** All three modified files above.

- [ ] **Step 1: Stage the three changed files**

  ```bash
  git add template/scheduled-tasks/ops/session-hygiene.md
  git add template/scheduled-tasks/ops/audit.md
  git add audits/AUD-2026-04-26-003.md
  ```

- [ ] **Step 2: Verify staged diff before committing**

  Run `git diff --staged` and confirm:
  - `session-hygiene.md`: exactly 2 lines changed (`!![op]` → `!! [op]`)
  - `audit.md`: exactly 1 line changed (step 0 parenthetical extended)
  - `AUD-2026-04-26-003.md`: exactly 6 lines changed (3 Status fields + 3 action items)
  - No other files staged

- [ ] **Step 3: Commit**

  ```bash
  git commit -m "fix: resolve AUD-052, AUD-053, and AUD-054 from AUD-2026-04-26-003"
  ```

  Expected output:
  ```
  [master <hash>] fix: resolve AUD-052, AUD-053, and AUD-054 from AUD-2026-04-26-003
   3 files changed, ...
  ```
