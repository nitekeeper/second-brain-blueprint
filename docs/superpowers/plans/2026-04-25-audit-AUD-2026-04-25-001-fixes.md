# Audit AUD-2026-04-25-001 Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix all 11 findings from audit report AUD-2026-04-25-001 across 7 blueprint files, update finding statuses, and commit.

**Architecture:** File-by-file batch — all edits for a given file are applied in one pass. Each task is one file. A final task updates the audit report status and commits everything in a single commit.

**Tech Stack:** Markdown file edits only. No code changes, no tests — these are documentation fixes.

---

## Task 1: Fix `user-guide.md` (findings 001, 002, 004, 005, 006 + sweep)

**Files:**
- Modify: `user-guide.md`

- [ ] **Step 1: Apply finding 002 — reconcile token figure on line 14**

  Find (line 14):
  ```
  If you saved a session summary with `!! wrap`, say `!! ready` at the start of your next session — the agent will load and read that summary before clearing it (~1,680 tokens total; the summary is compressed to ≤600 tokens so startup stays lean).
  ```
  Replace with:
  ```
  If you saved a session snapshot with `!! wrap`, say `!! ready` at the start of your next session — the agent will load and restore it before clearing it (~1,830 tokens total).
  ```

- [ ] **Step 2: Apply finding 005 — replace "compressed summary / 600-token cap" paragraph at line 142**

  Find (line 142):
  ```
  At the end of a productive session, say `!! wrap`. The agent will ask if there's anything specific to include, then write a **compressed** summary to `memory.md` — capturing the current task thread, the next action, any non-obvious decisions, and files changed. The format is intentionally minimal: the wiki stores permanent knowledge; session memory only bridges the current task thread from one session to the next. The summary is capped at ~600 tokens so loading it in the next session stays lean.
  ```
  Replace with:
  ```
  At the end of a productive session, say `!! wrap`. The agent will ask if there's anything specific to include, then write a compact context snapshot to `memory.md` capturing the task in flight, exact stopping point, next action, locked decisions, and active files. The format is machine-oriented and intentionally terse — the wiki stores permanent knowledge; the snapshot only bridges the current task thread to the next session.
  ```

- [ ] **Step 3: Apply sweep — fix stray "summary" instances in line 146**

  Find (line 146):
  ```
  Each `!! wrap` overwrites the previous summary, so only one summary exists at a time. **If a prior wrapped summary — or a truncated summary you preserved via `!! ready` → `keep` — is still in `memory.md`, the agent will warn you before overwriting.** Reply `no` to cancel, then consume or clear the existing content first.
  ```
  Replace with:
  ```
  Each `!! wrap` overwrites the previous snapshot, so only one snapshot exists at a time. **If a prior wrapped snapshot — or a truncated snapshot you preserved via `!! ready` → `keep` — is still in `memory.md`, the agent will warn you before overwriting.** Reply `no` to cancel, then consume or clear the existing content first.
  ```

- [ ] **Step 4: Apply finding 001 CRITICAL — fix `!! ready` verbatim display description at line 148**

  Find (line 148):
  ```
  At the start of your next session, say `!! ready` as your first message. The agent will display the summary in full (verbatim) to bring you back up to speed, then immediately wipe `memory.md` and confirm it's clear. From that point, the session proceeds normally.
  ```
  Replace with:
  ```
  At the start of your next session, say `!! ready` as your first message. The agent will silently parse the snapshot to restore context, then announce: "Resuming: [task]. Next: [next action]. Ready to continue." It will then wipe `memory.md` and confirm it's clear. From that point, the session proceeds normally.
  ```

- [ ] **Step 5: Apply sweep — fix stray "summary" in mid-session safeguard (line 150)**

  Find:
  ```
  This prevents the older footgun where a casual "ready" wiped a saved summary.
  ```
  Replace with:
  ```
  This prevents the older footgun where a casual "ready" wiped a saved snapshot.
  ```

- [ ] **Step 6: Apply sweep — fix stray "summary" in truncation detection paragraph (line 152)**

  Find:
  ```
  **Truncation detection:** If `!! wrap` was interrupted before writing the completion marker, `!! ready` will display what's present, warn that the summary appears truncated, and ask you to choose one of three options — `clear` (wipe back to empty), `keep` (mark the partial summary as acknowledged so the warning stops firing), or `edit` (hand the file back untouched for manual repair). It will never silently wipe truncated content. In schema v1.14+, `clear` and `keep` each log their recovery choice to `log.md` and refresh `hot.md`, so the audit trail stays intact; `edit` leaves the file untouched and writes nothing.
  ```
  Replace with:
  ```
  **Truncation detection:** If `!! wrap` was interrupted before writing the completion marker, `!! ready` will display what's present, warn that the snapshot appears truncated, and ask you to choose one of three options — `clear` (wipe back to empty), `keep` (mark the partial snapshot as acknowledged so the warning stops firing), or `edit` (hand the file back untouched for manual repair). It will never silently wipe truncated content. In schema v1.14+, `clear` and `keep` each log their recovery choice to `log.md` and refresh `hot.md`, so the audit trail stays intact; `edit` leaves the file untouched and writes nothing.
  ```

- [ ] **Step 7: Apply sweep — fix stray "summary" in last paragraph (line 154)**

  Find:
  ```
  If you start a session without saying `!! ready`, the summary stays in `memory.md` untouched until you explicitly ask for it. It won't be read automatically.
  ```
  Replace with:
  ```
  If you start a session without saying `!! ready`, the snapshot stays in `memory.md` untouched until you explicitly ask for it. It won't be read automatically.
  ```

- [ ] **Step 8: Apply sweep — fix stray "summary" in Note at line 156**

  Find:
  ```
  > **Note:** This is not a history log — it's a single-use memory bridge. Once consumed with `!! ready`, the summary is gone. If you need a permanent record of a decision, ingest it or file it as an analysis page.
  ```
  Replace with:
  ```
  > **Note:** This is not a history log — it's a single-use memory bridge. Once consumed with `!! ready`, the snapshot is gone. If you need a permanent record of a decision, ingest it or file it as an analysis page.
  ```

- [ ] **Step 9: Apply finding 004 — fix footer description strings (lines 178–179)**

  Find:
  ```
  💾 !! wrap: [save session summary to memory]
  🔄 !! ready: [load session summary at start of new session]
  ```
  Replace with:
  ```
  💾 !! wrap: [save session snapshot to memory]
  🔄 !! ready: [load session snapshot at start of new session]
  ```

- [ ] **Step 10: Apply finding 006 — fix Approval Rule exception text (line 202)**

  Find:
  ```
  `!! wrap` warns before overwriting an existing summary
  ```
  Replace with:
  ```
  `!! wrap` warns before overwriting an existing snapshot
  ```

- [ ] **Step 11: Apply finding 002 — reconcile token table entry (line 230)**

  Find:
  ```
  | `!! ready` with full memory | ~1,680 |
  ```
  Replace with:
  ```
  | `!! ready` with full memory | ~1,830 |
  ```

- [ ] **Step 12: Verify — grep for stray "summary" in session memory context**

  Run: `grep -n "summary" user-guide.md`

  Expected: no remaining hits that refer to `!! wrap`/`!! ready` session memory. Any hits for "one-line summary" (hot.md context), "executive summary", or unrelated uses are acceptable.

---

## Task 2: Fix `troubleshooting.md` (findings 007, 008 + sweep)

**Files:**
- Modify: `troubleshooting.md`

- [ ] **Step 1: Apply finding 007 — fix "session ended before !! wrap" section heading and body (line 147–149)**

  Find:
  ```
  ## Session ended before `!! wrap` completed

  **Symptom:** You tried to save a session summary but the session closed or timed out before the agent finished writing `memory.md`. The file is empty or incomplete next session.
  ```
  Replace with:
  ```
  ## Session ended before `!! wrap` completed

  **Symptom:** You tried to save a session snapshot but the session closed or timed out before the agent finished writing `memory.md`. The file is empty or incomplete next session.
  ```

- [ ] **Step 2: Apply finding 007 — fix mid-session section body (line 161)**

  Find:
  ```
  **Symptom:** You said something like "I'm ready" or typed `!! ready` during a session (not at the start), and the agent read and wiped `memory.md`, destroying the saved summary before you intended.
  ```
  Replace with:
  ```
  **Symptom:** You said something like "I'm ready" or typed `!! ready` during a session (not at the start), and the agent read and wiped `memory.md`, destroying the saved snapshot before you intended.
  ```

- [ ] **Step 3: Apply finding 007 — fix mid-session Fix line (line 165)**

  Find (under `## !! ready was triggered mid-session...`):
  ```
  **Fix:** The summary is gone and cannot be recovered.
  ```
  Replace with:
  ```
  **Fix:** The snapshot is gone and cannot be recovered.
  ```

- [ ] **Step 4: Apply finding 007 — fix overwrite section heading (line 171)**

  Find:
  ```
  ## `!! wrap` overwrote an existing session summary
  ```
  Replace with:
  ```
  ## `!! wrap` overwrote an existing session snapshot
  ```

- [ ] **Step 5: Apply finding 007 — fix overwrite section Symptom (line 173)**

  Find:
  ```
  **Symptom:** You invoked `!! wrap` and discovered afterward that a previous session summary (that you hadn't consumed with `!! ready`) was overwritten and lost.
  ```
  Replace with:
  ```
  **Symptom:** You invoked `!! wrap` and discovered afterward that a previous session snapshot (that you hadn't consumed with `!! ready`) was overwritten and lost.
  ```

- [ ] **Step 6: Apply finding 007 — fix overwrite section Fix line (line 177)**

  Find (under `## !! wrap overwrote an existing session snapshot`):
  ```
  **Fix:** The previous summary is gone and cannot be recovered.
  ```
  Replace with:
  ```
  **Fix:** The previous snapshot is gone and cannot be recovered.
  ```

- [ ] **Step 7: Apply finding 007 — fix overwrite section Prevention paragraph (line 179)**

  Find:
  ```
  **Prevention:** Schema v1.10+ requires explicit user confirmation when `!! wrap` detects a prior `MEMORY_STATE: WRAPPED` marker in `memory.md`. The current schema also catches `MEMORY_STATE: TRUNCATED_ACKNOWLEDGED` — a summary you preserved via `!! ready` → `keep` — with the same overwrite warning, so `keep` is no longer a one-shot preservation that the next `!! wrap` can silently destroy. If you see the overwrite warning, say `no` if you need to preserve the existing content first — consume it with `!! ready` in the current session, then `!! wrap` fresh.
  ```
  Replace with:
  ```
  **Prevention:** Schema v1.10+ requires explicit user confirmation when `!! wrap` detects a prior `MEMORY_STATE: WRAPPED` marker in `memory.md`. The current schema also catches `MEMORY_STATE: TRUNCATED_ACKNOWLEDGED` — a snapshot you preserved via `!! ready` → `keep` — with the same overwrite warning, so `keep` is no longer a one-shot preservation that the next `!! wrap` can silently destroy. If you see the overwrite warning, say `no` if you need to preserve the existing content first — consume it with `!! ready` in the current session, then `!! wrap` fresh.
  ```

- [ ] **Step 8: Apply finding 008 — fix truncation section Symptom (line 185)**

  Find:
  ```
  **Symptom:** `!! ready` next session shows only partial content and warns that the summary appears incomplete.
  ```
  Replace with:
  ```
  **Symptom:** `!! ready` next session shows only partial content and warns that the snapshot appears incomplete.
  ```

- [ ] **Step 9: Apply finding 008 — fix quoted log entry strings (line 189)**

  Find:
  ```
  `clear` and `keep` each append a `memory | Truncated summary cleared` or `memory | Truncated summary acknowledged` entry to `log.md` and refresh `hot.md`, so the recovery choice is visible in the operational trail; `edit` remains a no-op.
  ```
  Replace with:
  ```
  `clear` and `keep` each append a `memory | Truncated snapshot cleared` or `memory | Truncated snapshot acknowledged` entry to `log.md` and refresh `hot.md`, so the recovery choice is visible in the operational trail; `edit` remains a no-op.
  ```

- [ ] **Step 10: Verify — grep for stray "summary" in session memory context**

  Run: `grep -n "summary" troubleshooting.md`

  Expected: no remaining hits that refer to `!! wrap`/`!! ready` session memory. Hits for "executive summary" or unrelated uses are acceptable.

---

## Task 3: Fix `README.md` (finding 003)

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Apply finding 003 — fix "Session memory" feature description (line 78)**

  Find:
  ```
  - **Session memory** — `!! wrap` saves a detailed summary at session end; `!! ready` loads and clears it next session
  ```
  Replace with:
  ```
  - **Session memory** — `!! wrap` saves a compact session snapshot at end of session; `!! ready` restores working context at the start of the next session
  ```

- [ ] **Step 2: Verify**

  Run: `grep -n "summary" README.md`

  Expected: no hits referring to `!! wrap`/`!! ready` session memory.

---

## Task 4: Fix `setup-guide.md` (finding 004)

**Files:**
- Modify: `setup-guide.md`

- [ ] **Step 1: Apply finding 004 extra — fix memory.md initial content block (line 206)**

  Find:
  ```
  *(empty — use `!! wrap` at the end of a session to save a summary here)*
  ```
  Replace with:
  ```
  *(empty — use `!! wrap` at the end of a session to save a snapshot here)*
  ```

- [ ] **Step 2: Apply finding 004 — fix footer strings (lines 296–297)**

  Find:
  ```
  💾 !! wrap: [save session summary to memory]
  🔄 !! ready: [load session summary at start of new session]
  ```
  Replace with:
  ```
  💾 !! wrap: [save session snapshot to memory]
  🔄 !! ready: [load session snapshot at start of new session]
  ```

- [ ] **Step 3: Verify**

  Run: `grep -n "summary" setup-guide.md`

  Expected: no hits referring to `!! wrap`/`!! ready` session memory.

---

## Task 5: Fix `template/scheduled-tasks/ops/migrate.md` (finding 010)

**Files:**
- Modify: `template/scheduled-tasks/ops/migrate.md`

- [ ] **Step 1: Apply finding 010 — remove conventions.md from UNTOUCHED list and strip stray asterisk**

  Find (line 59):
  ```
     scheduled-tasks/ops/conventions.md, ingest.md* (beyond advisory update)
  ```
  Replace with:
  ```
     ingest.md (beyond advisory update)
  ```

- [ ] **Step 2: Verify — confirm conventions.md appears only in UPDATED**

  Run: `grep -n "conventions" template/scheduled-tasks/ops/migrate.md`

  Expected: exactly one hit, on the UPDATED line (`scheduled-tasks/ops/conventions.md (updated for v2.2)`). No hit in the UNTOUCHED block.

---

## Task 6: Fix `template/scheduled-tasks/ops/reference.md` (finding 011)

**Files:**
- Modify: `template/scheduled-tasks/ops/reference.md`

- [ ] **Step 1: Apply finding 011 — add documents/ to blueprint directory tree**

  Find (line 15–16 in the tree):
  ```
  ├── audits/                     ← Blueprint audit reports. Named AUD-YYYY-MM-DD-NNN.md. Developer use only.
  ├── backups/                    ← Migration backups. Gitignored. e.g. CLAUDE.md-v2.1-YYYY-MM-DD.bak
  ```
  Replace with:
  ```
  ├── audits/                     ← Blueprint audit reports. Named AUD-YYYY-MM-DD-NNN.md. Developer use only.
  ├── documents/                  ← Audit report template and design specifications. Developer use only.
  ├── backups/                    ← Migration backups. Gitignored. e.g. CLAUDE.md-v2.1-YYYY-MM-DD.bak
  ```

- [ ] **Step 2: Verify**

  Run: `grep -n "documents" template/scheduled-tasks/ops/reference.md`

  Expected: one hit — the new `documents/` tree entry.

---

## Task 7: Fix `skills/claude-code-enhanced/slash-commands.md` (finding 009)

**Files:**
- Modify: `skills/claude-code-enhanced/slash-commands.md`

- [ ] **Step 1: Apply finding 009 — add developer-only comment above /sync**

  Find:
  ```
  ## /sync

  Equivalent to triggering the blueprint sync rule. Read `@scheduled-tasks/ops/blueprint-sync.md` and confirm which files need updating based on the change just made.
  ```
  Replace with:
  ```
  ---

  > **Blueprint-authoring only:** `/sync` is intentionally not listed in SKILL.md or user-guide.md. It is a developer tool for blueprint maintainers — use it when editing blueprint files to check cascade requirements. Regular wiki users do not need it.

  ## /sync

  Equivalent to triggering the blueprint sync rule. Read `@scheduled-tasks/ops/blueprint-sync.md` and confirm which files need updating based on the change just made.
  ```

- [ ] **Step 2: Verify the file header still lists only the three user-facing commands**

  Run: `head -5 skills/claude-code-enhanced/slash-commands.md`

  Expected first line: `# Claude Code Enhanced — Slash Command Wrappers`
  Expected second content line: `This file is read when the user invokes `/wrap`, `/ready`, or `/migrate` in Claude Code CLI.`

  The header must NOT mention `/sync`.

---

## Task 8: Update audit report status and commit

**Files:**
- Modify: `audits/AUD-2026-04-25-001.md`
- Commit: all 8 files

- [ ] **Step 1: Mark all 11 findings RESOLVED in the audit report**

  For each of the 11 finding blocks, find:
  ```
  | **Status** | OPEN |
  ```
  Replace with:
  ```
  | **Status** | RESOLVED |
  ```
  (Do this for all 11 occurrences — findings AUD-2026-04-25-001 through AUD-2026-04-25-011.)

- [ ] **Step 2: Check all action item checkboxes**

  Find the Action Items section. Replace every `- [ ]` with `- [x]` for all 11 lines.

- [ ] **Step 3: Update the Executive Summary totals table**

  Find:
  ```
  | CRITICAL | 1 | 0 | 1 | 0 |
  | WARNING  | 10 | 0 | 10 | 0 |
  | STYLE    | 0 | 0 | 0 | 0 |
  | **Total**| **11** | **0** | **11** | **0** |
  ```
  Replace with:
  ```
  | CRITICAL | 1 | 0 | 0 | 1 |
  | WARNING  | 10 | 0 | 0 | 10 |
  | STYLE    | 0 | 0 | 0 | 0 |
  | **Total**| **11** | **0** | **0** | **11** |
  ```

- [ ] **Step 4: Update Overall Risk**

  Find:
  ```
  **Overall Risk:** `CRITICAL`
  ```
  Replace with:
  ```
  **Overall Risk:** `RESOLVED`
  ```

- [ ] **Step 5: Commit all changes**

  ```bash
  git add user-guide.md troubleshooting.md README.md setup-guide.md \
    template/scheduled-tasks/ops/migrate.md \
    template/scheduled-tasks/ops/reference.md \
    skills/claude-code-enhanced/slash-commands.md \
    audits/AUD-2026-04-25-001.md
  git commit -m "fix: resolve all 11 findings from audit AUD-2026-04-25-001

  - CRITICAL (001): user-guide !! ready now describes silent parse + two-line
    announcement instead of verbatim display
  - WARNING (002): reconcile cold-start token estimate to ~1,830 in both locations
  - WARNING (003): README session memory description updated to snapshot terminology
  - WARNING (004): footer strings updated to 'snapshot' in setup-guide and user-guide
  - WARNING (005): user-guide wrap description updated to compact snapshot (no 600-token cap)
  - WARNING (006): approval rule exception updated to 'existing snapshot'
  - WARNING (007): troubleshooting summary→snapshot terminology throughout
  - WARNING (008): quoted log entry strings updated to 'Truncated snapshot'
  - WARNING (009): /sync marked developer-only with comment in slash-commands.md
  - WARNING (010): conventions.md removed from migrate.md UNTOUCHED list
  - WARNING (011): documents/ added to reference.md directory tree"
  ```

- [ ] **Step 6: Verify commit**

  Run: `git log --oneline -3`

  Expected: the new commit appears at the top with the message above.
