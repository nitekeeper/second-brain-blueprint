# AUD-2026-04-25-005 Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Apply all four findings from AUD-2026-04-25-005 — two CRITICALs in migrate.md, one WARNING in blueprint-sync.md, one WARNING in audit.md — and mark all findings RESOLVED in the audit report, in a single commit.

**Architecture:** Four targeted text edits to three blueprint ops files, followed by a status update to the audit report. No code is executed; verification is done by reading the edited sections back and confirming the change matches the spec. Blueprint-authoring mode is active (no `wiki/` at root) — skip all `wiki/log.md` appends and `hot.md` refreshes.

**Tech Stack:** Markdown file edits only. Edit tool for surgical replacements. Git for the final commit.

---

## Files Modified

| File | Findings | Change |
|---|---|---|
| `template/scheduled-tasks/ops/migrate.md` | AUD-023, AUD-024 | Insert missing Step 2 branch; add step c2; update approval block |
| `template/scheduled-tasks/ops/blueprint-sync.md` | AUD-025 | Two new rows in cascade table |
| `template/scheduled-tasks/ops/audit.md` | AUD-026 | Update estimate command + token range in Notes |
| `audits/AUD-2026-04-25-005.md` | All 4 | Status → RESOLVED; action items checked off |

---

## Task 1: Fix AUD-023 — Insert missing Step 2 third branch in migrate.md

**Files:**
- Modify: `template/scheduled-tasks/ops/migrate.md` (step c replacement block, Step 2 section)

- [ ] **Step 1: Read the current Step 2 block in migrate.md step c**

  Read `template/scheduled-tasks/ops/migrate.md` lines 78–83. Confirm the Step 2 block currently has exactly two bullets:
  ```
  - If the result **directly answered the question** → silently save to `wiki/inbox/` and read `@scheduled-tasks/ops/ingest.md` to ingest it
  - If the result is **loosely related but did not answer** → skip ingest; use partial findings to inform Step 3
  ```

- [ ] **Step 2: Insert the missing third bullet**

  Using the Edit tool, replace the two-bullet Step 2 block with the three-bullet version. The exact old string to match:

  ```
  - If the result **directly answered the question** → silently save to `wiki/inbox/` and read `@scheduled-tasks/ops/ingest.md` to ingest it
  - If the result is **loosely related but did not answer** → skip ingest; use partial findings to inform Step 3
  ```

  Replace with:

  ```
  - If the result **directly answered the question** → silently save to `wiki/inbox/` and read `@scheduled-tasks/ops/ingest.md` to ingest it
  - If the result **directly answered the question AND Step 1 returned partial wiki content** → ingest the web result; answer citing both the wiki pages and the web result
  - If the result is **loosely related but did not answer** → skip ingest; use partial findings to inform Step 3
  ```

- [ ] **Step 3: Verify**

  Read `template/scheduled-tasks/ops/migrate.md` lines 78–87. Confirm the Step 2 block now has three bullets in order: "directly answered", "directly answered AND partial wiki", "loosely related". Confirm the rest of the step c block is unchanged.

---

## Task 2: Fix AUD-024 — Add conventions.md copy step and update approval request in migrate.md

**Files:**
- Modify: `template/scheduled-tasks/ops/migrate.md` (approval request block + execution steps)

- [ ] **Step 1: Update the approval request block**

  Using the Edit tool, replace the entire UPDATED+UNTOUCHED block in the approval request. The exact old string to match (inside the fenced code block in the approval request section):

  ```
  Files UPDATED:
    CLAUDE.md  (Query Routing Rule section replaced — behavior change, no size change)

  Files UNTOUCHED:
    wiki/  (all pages, index, log, hot preserved)
    memory.md, raw/, drafts/
    scheduled-tasks/ops/  (all ops files preserved)
    scripts/  (all scripts preserved)
    sqlite-query skill (if installed)
  ```

  Replace with:

  ```
  Files UPDATED:
    CLAUDE.md  (Query Routing Rule section replaced — behavior change, no size change)
    scheduled-tasks/ops/conventions.md  (Filing Answers step labels updated to v2.3 numbering)

  Files UNTOUCHED:
    wiki/  (all pages, index, log, hot preserved)
    memory.md, raw/, drafts/
    scheduled-tasks/ops/  (all other ops files preserved)
    scripts/  (all scripts preserved)
    sqlite-query skill (if installed)
  ```

- [ ] **Step 2: Insert step c2 after step c**

  Using the Edit tool, find the line that begins step d (the line that reads `d. Read \`CLAUDE.md\` footer`) and insert a new step c2 immediately before it. The exact old string to match:

  ```
  d. Read `CLAUDE.md` footer (last non-empty line). If it already reads
  ```

  Replace with:

  ```
  c2. Copy `blueprint/template/scheduled-tasks/ops/conventions.md` → `scheduled-tasks/ops/conventions.md`
  d. Read `CLAUDE.md` footer (last non-empty line). If it already reads
  ```

- [ ] **Step 3: Verify**

  Read `template/scheduled-tasks/ops/migrate.md` lines 40–110. Confirm:
  1. Approval request block shows `conventions.md` under Files UPDATED and says "(all other ops files preserved)" in UNTOUCHED.
  2. Step c2 appears between step c's closing ``` fence and step d, with the exact copy command.
  3. Steps d, e, f remain unchanged and in order.

---

## Task 3: Fix AUD-025 — Add two cascade rows to blueprint-sync.md

**Files:**
- Modify: `template/scheduled-tasks/ops/blueprint-sync.md` (cascade table)

- [ ] **Step 1: Read the cascade table**

  Read `template/scheduled-tasks/ops/blueprint-sync.md`. Identify the last row of the cascade table. Confirm the table currently ends with:

  ```
  | New skill bundle added | `blueprint/skills/<skill>/` + `blueprint/user-guide.md` + `blueprint/setup-guide.md` + `blueprint/ROADMAP.md` + `ops/conventions.md` if skill introduces a new hook contract |
  ```

- [ ] **Step 2: Append two new rows**

  Using the Edit tool, replace the last table row with the last row plus the two new rows. The exact old string to match:

  ```
  | New skill bundle added | `blueprint/skills/<skill>/` + `blueprint/user-guide.md` + `blueprint/setup-guide.md` + `blueprint/ROADMAP.md` + `ops/conventions.md` if skill introduces a new hook contract |
  ```

  Replace with:

  ```
  | New skill bundle added | `blueprint/skills/<skill>/` + `blueprint/user-guide.md` + `blueprint/setup-guide.md` + `blueprint/ROADMAP.md` + `ops/conventions.md` if skill introduces a new hook contract |
  | Query Routing Rule change | `blueprint/template/CLAUDE.md` (always) + `blueprint/template/scheduled-tasks/ops/migrate.md` (update the step c replacement block in the active migration path to match the new rule) |
  | Ops file change that is copied by the v2.1→v2.3 migration step g | `blueprint/template/scheduled-tasks/ops/migrate.md` (verify the updated file is listed in step g; add it if absent) |
  ```

- [ ] **Step 3: Verify**

  Read `template/scheduled-tasks/ops/blueprint-sync.md`. Confirm the table now has two new rows at the bottom for "Query Routing Rule change" and "Ops file change that is copied by the v2.1→v2.3 migration step g". Confirm all preceding rows are unchanged.

---

## Task 4: Fix AUD-026 — Update estimate command and range in audit.md

**Files:**
- Modify: `template/scheduled-tasks/ops/audit.md` (Notes section)

- [ ] **Step 1: Read the Notes section**

  Read `template/scheduled-tasks/ops/audit.md` lines 90–96. Confirm the estimate line currently reads:

  ```
  For `!! audit all`, expect ~30,000–47,000 tokens of reads for the tracked files. Run `python scripts/estimate_tokens.py blueprint/README.md blueprint/setup-guide.md blueprint/user-guide.md blueprint/troubleshooting.md blueprint/template/CLAUDE.md blueprint/template/scheduled-tasks/refresh-hot.md blueprint/template/scheduled-tasks/ops/*.md blueprint/skills/sqlite-query/*.md blueprint/skills/claude-code-enhanced/*.md` for a live estimate. Warn the user up front if the session is already close to context limits.
  ```

- [ ] **Step 2: Replace the estimate line**

  Using the Edit tool, replace the old estimate line with the updated one. The exact old string to match:

  ```
  For `!! audit all`, expect ~30,000–47,000 tokens of reads for the tracked files. Run `python scripts/estimate_tokens.py blueprint/README.md blueprint/setup-guide.md blueprint/user-guide.md blueprint/troubleshooting.md blueprint/template/CLAUDE.md blueprint/template/scheduled-tasks/refresh-hot.md blueprint/template/scheduled-tasks/ops/*.md blueprint/skills/sqlite-query/*.md blueprint/skills/claude-code-enhanced/*.md` for a live estimate. Warn the user up front if the session is already close to context limits.
  ```

  Replace with:

  ```
  For `!! audit all`, expect ~60,000–70,000 tokens of reads for the tracked files (CHANGELOG.md alone accounts for ~30,000+ tokens and grows with every audit cycle). Run `python scripts/estimate_tokens.py blueprint/README.md blueprint/setup-guide.md blueprint/user-guide.md blueprint/troubleshooting.md blueprint/CHANGELOG.md blueprint/template/CLAUDE.md blueprint/template/scheduled-tasks/refresh-hot.md blueprint/template/scheduled-tasks/ops/*.md blueprint/skills/sqlite-query/*.md blueprint/skills/claude-code-enhanced/*.md` for a live estimate. Warn the user up front if the session is already close to context limits.
  ```

- [ ] **Step 3: Verify**

  Read `template/scheduled-tasks/ops/audit.md` Notes section. Confirm:
  1. Range now reads `~60,000–70,000`.
  2. `blueprint/CHANGELOG.md` appears in the estimate command between `blueprint/troubleshooting.md` and `blueprint/template/CLAUDE.md`.
  3. The parenthetical about CHANGELOG.md growing is present.
  4. All other Notes content is unchanged.

---

## Task 5: Mark all findings RESOLVED in AUD-2026-04-25-005.md

**Files:**
- Modify: `audits/AUD-2026-04-25-005.md`

- [ ] **Step 1: Update AUD-023 status**

  Using the Edit tool, replace:
  ```
  | **Status** | OPEN |
  | **File** | `template/scheduled-tasks/ops/migrate.md` |
  | **Location** | v2.2→v2.3 path, step c, Step 2 block |
  ```
  With:
  ```
  | **Status** | RESOLVED |
  | **File** | `template/scheduled-tasks/ops/migrate.md` |
  | **Location** | v2.2→v2.3 path, step c, Step 2 block |
  ```

- [ ] **Step 2: Update AUD-024 status**

  Using the Edit tool, replace:
  ```
  | **Status** | OPEN |
  | **File** | `template/scheduled-tasks/ops/migrate.md` |
  | **Location** | v2.2→v2.3 path — approval request "Files UNTOUCHED" block; step c list |
  ```
  With:
  ```
  | **Status** | RESOLVED |
  | **File** | `template/scheduled-tasks/ops/migrate.md` |
  | **Location** | v2.2→v2.3 path — approval request "Files UNTOUCHED" block; step c list |
  ```

- [ ] **Step 3: Update AUD-025 status**

  Using the Edit tool, replace:
  ```
  | **Status** | OPEN |
  | **File** | `template/scheduled-tasks/ops/blueprint-sync.md` |
  | **Location** | Cascade table |
  ```
  With:
  ```
  | **Status** | RESOLVED |
  | **File** | `template/scheduled-tasks/ops/blueprint-sync.md` |
  | **Location** | Cascade table |
  ```

- [ ] **Step 4: Update AUD-026 status**

  Using the Edit tool, replace:
  ```
  | **Status** | OPEN |
  | **File** | `template/scheduled-tasks/ops/audit.md` |
  | **Location** | Notes section — estimate command and token range |
  ```
  With:
  ```
  | **Status** | RESOLVED |
  | **File** | `template/scheduled-tasks/ops/audit.md` |
  | **Location** | Notes section — estimate command and token range |
  ```

- [ ] **Step 5: Check off all action items**

  Using the Edit tool, replace:
  ```
  - [ ] `AUD-2026-04-25-023` **[CRITICAL]** `migrate.md` v2.2→v2.3 step c missing AUD-019 third Step 2 branch — Owner: ___ | Target: ___
  - [ ] `AUD-2026-04-25-024` **[CRITICAL]** `migrate.md` v2.2→v2.3 doesn't re-copy `conventions.md` after AUD-018 — Owner: ___ | Target: ___
  - [ ] `AUD-2026-04-25-025` **[WARNING]** `blueprint-sync.md` missing `migrate.md` cascade rule for routing-rule changes — Owner: ___ | Target: ___
  - [ ] `AUD-2026-04-25-026` **[WARNING]** `audit.md` estimate command and range omit `CHANGELOG.md` — Owner: ___ | Target: ___
  ```
  With:
  ```
  - [x] `AUD-2026-04-25-023` **[CRITICAL]** `migrate.md` v2.2→v2.3 step c missing AUD-019 third Step 2 branch — Owner: ___ | Target: ___
  - [x] `AUD-2026-04-25-024` **[CRITICAL]** `migrate.md` v2.2→v2.3 doesn't re-copy `conventions.md` after AUD-018 — Owner: ___ | Target: ___
  - [x] `AUD-2026-04-25-025` **[WARNING]** `blueprint-sync.md` missing `migrate.md` cascade rule for routing-rule changes — Owner: ___ | Target: ___
  - [x] `AUD-2026-04-25-026` **[WARNING]** `audit.md` estimate command and range omit `CHANGELOG.md` — Owner: ___ | Target: ___
  ```

- [ ] **Step 6: Verify**

  Read `audits/AUD-2026-04-25-005.md`. Confirm all four finding Status fields read `RESOLVED` and all four action item checkboxes are checked (`[x]`).

---

## Task 6: Commit all changes

- [ ] **Step 1: Stage all modified files**

  ```bash
  git add template/scheduled-tasks/ops/migrate.md \
          template/scheduled-tasks/ops/blueprint-sync.md \
          template/scheduled-tasks/ops/audit.md \
          audits/AUD-2026-04-25-005.md
  ```

- [ ] **Step 2: Verify staged files**

  Run:
  ```bash
  git diff --staged --name-only
  ```
  Expected output (4 files, no others):
  ```
  audits/AUD-2026-04-25-005.md
  template/scheduled-tasks/ops/audit.md
  template/scheduled-tasks/ops/blueprint-sync.md
  template/scheduled-tasks/ops/migrate.md
  ```

- [ ] **Step 3: Commit**

  ```bash
  git commit -m "fix: apply AUD-2026-04-25-005 findings (023-026), mark all RESOLVED

  Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
  ```

- [ ] **Step 4: Verify commit**

  Run:
  ```bash
  git show --stat HEAD
  ```
  Expected: commit touching exactly the 4 files above with the correct message.
