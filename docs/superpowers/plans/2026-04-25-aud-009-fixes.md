# AUD-2026-04-25-009 Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Apply all 5 audit findings from AUD-2026-04-25-009 plus one related fix in audit.md, then mark all findings RESOLVED in the audit report.

**Architecture:** Pure documentation edits — no code, no tests. Six targeted text replacements across five markdown files, followed by an audit report status update, all landed in a single commit.

**Tech Stack:** Markdown, git

---

## File Map

| File | Task | Change type |
|---|---|---|
| `skills/sqlite-query/query-layer.md` | Task 1 | Replace one sentence (step 3 fallback) |
| `template/scheduled-tasks/ops/migrate.md` | Task 2 | Insert one step; extend one bullet; update one message |
| `template/scheduled-tasks/ops/audit.md` | Task 3 | Append one scope entry; update token command; fix MEDIUM→LOW |
| `template/scheduled-tasks/ops/reference.md` | Task 4 | Insert two lines into directory tree |
| `docs/audit-report-template.md` | Task 5 | Replace one paragraph (risk derivation) |
| `audits/AUD-2026-04-25-009.md` | Task 6 | Set 5 statuses to RESOLVED; check 5 action items |

> **Note on TDD:** These are markdown documentation edits. There is no executable test suite. Verification for each task is a read-back check: after each edit, read the changed section and confirm the text matches the spec exactly.

---

### Task 1: Fix AUD-034 — query-layer.md ambiguous cross-reference

**Files:**
- Modify: `skills/sqlite-query/query-layer.md:67`

- [ ] **Step 1: Edit the file**

In `skills/sqlite-query/query-layer.md`, find step 3 (the `**If \`candidate_paths\` is empty**` block). The last sentence of that step currently reads:

```
If grep also returns nothing, proceed to Step 3 of the core query op (read `index.md`).
```

Replace that sentence with:

```
If grep also returns nothing, read `wiki/index.md` directly and continue from sub-step 4 of the query waterfall (read candidate pages; answer with [[wiki link]] citations).
```

- [ ] **Step 2: Verify**

Read `skills/sqlite-query/query-layer.md` lines 63–69. Confirm:
- The old phrase `proceed to Step 3 of the core query op` is gone
- The new sentence contains `read \`wiki/index.md\` directly` and `sub-step 4 of the query waterfall`
- No other lines in step 3 changed

---

### Task 2: Fix AUD-035 — migrate.md rollback omits blueprint-sync.md

**Files:**
- Modify: `template/scheduled-tasks/ops/migrate.md:63,117-118,222`

- [ ] **Step 1: Insert backup step b3**

In `template/scheduled-tasks/ops/migrate.md`, find the line:

```
b2. Copy `scheduled-tasks/ops/conventions.md` → `backups/conventions.md-v2.2-<YYYY-MM-DD>.bak`
```

Insert a new line immediately after it:

```
b3. Copy `scheduled-tasks/ops/blueprint-sync.md` → `backups/blueprint-sync.md-v2.2-<YYYY-MM-DD>.bak`
```

- [ ] **Step 2: Extend the Rollback bullet**

Find the Rollback section line that begins:

```
- **v2.2→v2.3:** Rename `backups/CLAUDE.md-v2.2-<date>.bak` → `CLAUDE.md`; rename `backups/conventions.md-v2.2-<date>.bak` → `scheduled-tasks/ops/conventions.md`; revert `Schema:` in `hot.md` to `v2.2`.
```

Replace it with:

```
- **v2.2→v2.3:** Rename `backups/CLAUDE.md-v2.2-<date>.bak` → `CLAUDE.md`; rename `backups/conventions.md-v2.2-<date>.bak` → `scheduled-tasks/ops/conventions.md`; rename `backups/blueprint-sync.md-v2.2-<date>.bak` → `scheduled-tasks/ops/blueprint-sync.md`; revert `Schema:` in `hot.md` to `v2.2`.
```

- [ ] **Step 3: Update the Confirm message**

Find the Confirm block:

```
> "Migration complete. Query routing is now wiki-first.
> Backups saved to `backups/CLAUDE.md-v2.2-<date>.bak` and `backups/conventions.md-v2.2-<date>.bak` — delete when satisfied."
```

Replace it with:

```
> "Migration complete. Query routing is now wiki-first.
> Backups saved to `backups/CLAUDE.md-v2.2-<date>.bak`, `backups/conventions.md-v2.2-<date>.bak`, and `backups/blueprint-sync.md-v2.2-<date>.bak` — delete when satisfied."
```

- [ ] **Step 4: Verify**

Read `template/scheduled-tasks/ops/migrate.md`. Confirm:
- Step b3 exists between b2 and c
- Rollback v2.2→v2.3 bullet contains `blueprint-sync.md-v2.2-<date>.bak` before `; revert Schema:`
- Confirm message lists three backup files joined by commas
- No other lines changed

---

### Task 3: Fix AUD-036 + related — audit.md scope and risk level

**Files:**
- Modify: `template/scheduled-tasks/ops/audit.md:24,48,95`

- [ ] **Step 1: Add docs/audit-report-template.md to scope list**

In `template/scheduled-tasks/ops/audit.md`, find the `!! audit all` scope list. The last bullet in the list currently reads:

```
- Every file under `blueprint/skills/` (skill bundles — recurse into subdirectories)
```

Append a new bullet immediately after it:

```
- `blueprint/docs/audit-report-template.md`
```

- [ ] **Step 2: Fix MEDIUM→LOW in Executive Summary step**

Find the Executive Summary sub-bullet in Step 4c. It currently contains:

```
open STYLEs only → `MEDIUM`
```

Change that fragment to:

```
open STYLEs only → `LOW`
```

The full bullet line should then read:
```
- **Executive Summary**: 2–3 sentences; overall risk level (CRITICAL if any open CRITICALs → `CRITICAL`; open WARNINGs only → `HIGH`; open STYLEs only → `LOW`; zero open findings → `CLEAN`); finding counts by severity broken into new vs. carried-over.
```

- [ ] **Step 3: Add file to token estimate command**

Find the Notes section token estimate command (the long `python scripts/estimate_tokens.py ...` line). It currently ends with:

```
blueprint/skills/claude-code-enhanced/*.md
```

Append `blueprint/docs/audit-report-template.md` so the command ends with:

```
blueprint/skills/claude-code-enhanced/*.md blueprint/docs/audit-report-template.md
```

- [ ] **Step 4: Verify**

Read `template/scheduled-tasks/ops/audit.md`. Confirm:
- `blueprint/docs/audit-report-template.md` appears in the scope list after the skills bullet
- The Executive Summary step says `open STYLEs only → \`LOW\`` (not MEDIUM)
- The token estimate command ends with `blueprint/docs/audit-report-template.md`
- No other lines changed

---

### Task 4: Fix AUD-037 — reference.md blueprint/ tree omits docs/

**Files:**
- Modify: `template/scheduled-tasks/ops/reference.md:35`

- [ ] **Step 1: Insert docs/ entry into the blueprint/ subtree**

In `template/scheduled-tasks/ops/reference.md`, find the blueprint/ subtree. Locate this line:

```
│   ├── ROADMAP.md
│   ├── template/
```

Insert two lines between them so the result reads:

```
│   ├── ROADMAP.md
│   ├── docs/                   ← Audit report template and design specs. Developer use only.
│   │   └── audit-report-template.md
│   ├── template/
```

- [ ] **Step 2: Verify**

Read `template/scheduled-tasks/ops/reference.md`. Confirm:
- `docs/` entry appears between `ROADMAP.md` and `template/` in the blueprint/ subtree
- `audit-report-template.md` is listed one level under `docs/`
- The arrow comment reads `← Audit report template and design specs. Developer use only.`
- No other lines changed

---

### Task 5: Fix AUD-038 — audit-report-template.md contradictory risk derivation

**Files:**
- Modify: `docs/audit-report-template.md:27`

- [ ] **Step 1: Replace the risk derivation note**

In `docs/audit-report-template.md`, find the Executive Summary section. The risk derivation blockquote currently reads:

```
> Risk is derived from the highest-severity open finding: any unresolved CRITICAL → CRITICAL; all resolved CRITICALs but open WARNINGs → HIGH; all resolved WARNINGs but open STYLEs → MEDIUM; only resolved or STYLE findings → LOW; zero findings → CLEAN.
```

Replace it with:

```
> Risk is derived from the highest-severity open finding in priority order: (1) any unresolved CRITICAL → CRITICAL; (2) open WARNINGs (no open CRITICALs) → HIGH; (3) only resolved findings and/or open STYLEs → LOW; (4) zero findings → CLEAN. STYLE findings do not elevate risk above LOW — they are documentation-only.
```

- [ ] **Step 2: Verify**

Read `docs/audit-report-template.md`. Confirm:
- The old phrase `all resolved WARNINGs but open STYLEs → MEDIUM` is gone
- The new derivation uses numbered priority order `(1)` through `(4)`
- The sentence `STYLE findings do not elevate risk above LOW — they are documentation-only.` is present
- No other lines changed

---

### Task 6: Update audit report — mark all findings RESOLVED

**Files:**
- Modify: `audits/AUD-2026-04-25-009.md`

- [ ] **Step 1: Set AUD-034 status to RESOLVED**

Find the AUD-034 finding table. Change:

```
| **Status** | OPEN |
```

to:

```
| **Status** | RESOLVED |
```

- [ ] **Step 2: Set AUD-035 status to RESOLVED**

Find the AUD-035 finding table. Change:

```
| **Status** | OPEN |
```

to:

```
| **Status** | RESOLVED |
```

- [ ] **Step 3: Set AUD-036 status to RESOLVED**

Find the AUD-036 finding table. Change:

```
| **Status** | OPEN |
```

to:

```
| **Status** | RESOLVED |
```

- [ ] **Step 4: Set AUD-037 status to RESOLVED**

Find the AUD-037 finding table. Change:

```
| **Status** | OPEN |
```

to:

```
| **Status** | RESOLVED |
```

- [ ] **Step 5: Set AUD-038 status to RESOLVED**

Find the AUD-038 finding table. Change:

```
| **Status** | OPEN |
```

to:

```
| **Status** | RESOLVED |
```

- [ ] **Step 6: Check off all Action Items**

In the Action Items section, change all five `- [ ]` entries to `- [x]`:

```
- [x] `AUD-2026-04-25-034` **[STYLE]** `query-layer.md` step 3 fallback cross-reference is ambiguous — Owner: ___ | Target: ___
- [x] `AUD-2026-04-25-035` **[STYLE]** `migrate.md` v2.2→v2.3 rollback omits `blueprint-sync.md` revert — Owner: ___ | Target: ___
- [x] `AUD-2026-04-25-036` **[STYLE]** `docs/audit-report-template.md` excluded from `audit.md` audit scope — Owner: ___ | Target: ___
- [x] `AUD-2026-04-25-037` **[STYLE]** `reference.md` blueprint/ directory tree omits `docs/` directory — Owner: ___ | Target: ___
- [x] `AUD-2026-04-25-038` **[STYLE]** `docs/audit-report-template.md` risk-level derivation is self-contradictory — Owner: ___ | Target: ___
```

- [ ] **Step 7: Verify**

Read `audits/AUD-2026-04-25-009.md`. Confirm:
- All 5 finding tables show `| **Status** | RESOLVED |`
- All 5 Action Items entries show `- [x]`
- No other lines changed

---

### Task 7: Single commit

**Files:** all six modified files

- [ ] **Step 1: Stage all changed files**

```bash
git add skills/sqlite-query/query-layer.md \
        template/scheduled-tasks/ops/migrate.md \
        template/scheduled-tasks/ops/audit.md \
        template/scheduled-tasks/ops/reference.md \
        docs/audit-report-template.md \
        audits/AUD-2026-04-25-009.md
```

- [ ] **Step 2: Confirm staged files**

```bash
git diff --staged --stat
```

Expected output: 6 files changed. Verify no unexpected files are staged.

- [ ] **Step 3: Commit**

```bash
git commit -m "fix: resolve AUD-034 through AUD-038 from AUD-2026-04-25-009"
```

Expected: commit succeeds with 6 files changed.

- [ ] **Step 4: Verify**

```bash
git show --stat HEAD
```

Confirm the commit message and all 6 files appear in the output.
