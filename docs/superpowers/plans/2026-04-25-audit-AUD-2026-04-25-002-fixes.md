# Audit AUD-2026-04-25-002 Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Resolve all 3 open findings from audit report AUD-2026-04-25-002 by making surgical edits to 3 blueprint files, then updating the audit report to reflect all findings as RESOLVED.

**Architecture:** Four-file batch edit — fix each file in sequence, update the audit report last, single commit covering all changes. No code or scripts are touched; all changes are markdown doc edits.

**Tech Stack:** Markdown, git

---

## File Map

| File | Change |
|---|---|
| `user-guide.md` | Remove duplicate token table row (finding 012) |
| `template/scheduled-tasks/ops/migrate.md` | Remove `ingest.md` from UNTOUCHED block (finding 013) |
| `template/scheduled-tasks/ops/audit.md` | Add `claude-code-enhanced` glob to estimate command (finding 014) |
| `audits/AUD-2026-04-25-002.md` | Mark all 3 findings RESOLVED, check action items, update risk to CLEAN |

---

### Task 1: Remove duplicate token table row from `user-guide.md`

**Files:**
- Modify: `user-guide.md` (~line 230)

- [ ] **Step 1: Verify the duplicate row exists**

Run:
```bash
grep -n "!! ready.*full memory" user-guide.md
```
Expected output (two lines, same value):
```
222:| Cold start with `!! ready` (full memory) | ~1,830 |
230:| `!! ready` with full memory | ~1,830 |
```

- [ ] **Step 2: Remove the duplicate row**

In `user-guide.md`, delete line 230 exactly as shown:

Remove this line:
```
| `!! ready` with full memory | ~1,830 |
```

The token table after the fix should end with:
```
| `!! wrap` (realistic) | ~1,500–2,500 |
```

The `Cold start with !! ready (full memory) | ~1,830` row at line 222 is kept — it is the canonical label.

- [ ] **Step 3: Verify only one row remains**

Run:
```bash
grep -n "!! ready.*full memory\|full memory.*1,830" user-guide.md
```
Expected: exactly one match at approximately line 222.

---

### Task 2: Remove `ingest.md` from UNTOUCHED block in `migrate.md`

**Files:**
- Modify: `template/scheduled-tasks/ops/migrate.md` (~line 59)

- [ ] **Step 1: Verify the duplicate entry exists**

Run:
```bash
grep -n "ingest.md" template/scheduled-tasks/ops/migrate.md
```
Expected output (two lines — once in UPDATED, once in UNTOUCHED):
```
52:    scheduled-tasks/ops/ingest.md      (bash → Python, post-op advisory added)
59:    ingest.md (beyond advisory update)
```

- [ ] **Step 2: Remove the UNTOUCHED entry**

In `template/scheduled-tasks/ops/migrate.md`, delete line 59 exactly as shown:

Remove this line:
```
    ingest.md (beyond advisory update)
```

The UNTOUCHED block after the fix should read:
```
   Files UNTOUCHED:
     wiki/  (all pages, index, log, hot preserved)
     memory.md, raw/, drafts/
     sqlite-query skill (if installed)
```

- [ ] **Step 3: Verify `ingest.md` appears only once**

Run:
```bash
grep -n "ingest.md" template/scheduled-tasks/ops/migrate.md
```
Expected: exactly one match (in the UPDATED block at approximately line 52).

---

### Task 3: Add `claude-code-enhanced` glob to estimate command in `audit.md`

**Files:**
- Modify: `template/scheduled-tasks/ops/audit.md` (~line 93)

- [ ] **Step 1: Verify the current estimate command**

Run:
```bash
grep -n "estimate_tokens.py" template/scheduled-tasks/ops/audit.md
```
Expected: one match on approximately line 93, ending with `blueprint/skills/sqlite-query/*.md\``.

- [ ] **Step 2: Update the estimate command**

In `template/scheduled-tasks/ops/audit.md`, replace the estimate command in the Notes section.

Old line (~line 93):
```
- For `!! audit all`, expect ~30,000–47,000 tokens of reads for the tracked files. Run `python scripts/estimate_tokens.py blueprint/README.md blueprint/setup-guide.md blueprint/user-guide.md blueprint/troubleshooting.md blueprint/template/CLAUDE.md blueprint/template/scheduled-tasks/refresh-hot.md blueprint/template/scheduled-tasks/ops/*.md blueprint/skills/sqlite-query/*.md` for a live estimate. Warn the user up front if the session is already close to context limits.
```

New line:
```
- For `!! audit all`, expect ~30,000–47,000 tokens of reads for the tracked files. Run `python scripts/estimate_tokens.py blueprint/README.md blueprint/setup-guide.md blueprint/user-guide.md blueprint/troubleshooting.md blueprint/template/CLAUDE.md blueprint/template/scheduled-tasks/refresh-hot.md blueprint/template/scheduled-tasks/ops/*.md blueprint/skills/sqlite-query/*.md blueprint/skills/claude-code-enhanced/*.md` for a live estimate. Warn the user up front if the session is already close to context limits.
```

- [ ] **Step 3: Verify the updated command**

Run:
```bash
grep -n "claude-code-enhanced" template/scheduled-tasks/ops/audit.md
```
Expected: one match on approximately line 93.

---

### Task 4: Update audit report `AUD-2026-04-25-002.md`

**Files:**
- Modify: `audits/AUD-2026-04-25-002.md`

- [ ] **Step 1: Update finding 012 status to RESOLVED**

In `audits/AUD-2026-04-25-002.md`, in the `[AUD-2026-04-25-012]` finding block, change:
```
| **Status** | OPEN |
```
to:
```
| **Status** | RESOLVED |
```

- [ ] **Step 2: Update finding 013 status to RESOLVED**

In the `[AUD-2026-04-25-013]` finding block, change:
```
| **Status** | OPEN |
```
to:
```
| **Status** | RESOLVED |
```

- [ ] **Step 3: Update finding 014 status to RESOLVED**

In the `[AUD-2026-04-25-014]` finding block, change:
```
| **Status** | OPEN |
```
to:
```
| **Status** | RESOLVED |
```

- [ ] **Step 4: Check all 3 action-item boxes**

In the Action Items section, change all three `- [ ]` entries to `- [x]`:

```
- [x] `AUD-2026-04-25-012` **[WARNING]** Token table duplicate rows for `!! ready` cold-start — Owner: ___ | Target: ___
- [x] `AUD-2026-04-25-013` **[WARNING]** `migrate.md` lists `ingest.md` in both UPDATED and UNTOUCHED — Owner: ___ | Target: ___
- [x] `AUD-2026-04-25-014` **[STYLE]** `audit.md` estimate command missing `claude-code-enhanced` files — Owner: ___ | Target: ___
```

- [ ] **Step 5: Update Overall Risk to CLEAN**

In the Executive Summary, change:
```
**Overall Risk:** `HIGH`
```
to:
```
**Overall Risk:** `CLEAN`
```

- [ ] **Step 6: Update the Executive Summary finding counts table**

Change the summary table to reflect 0 open, 3 resolved this cycle:

```
| Severity | New This Cycle | Carried Over | Total Open | Resolved This Cycle |
|---|---|---|---|---|
| CRITICAL | 0 | 0 | 0 | 0 |
| WARNING  | 2 | 0 | 0 | 2 |
| STYLE    | 1 | 0 | 0 | 1 |
| **Total**| **3** | **0** | **0** | **3** |
```

- [ ] **Step 7: Verify all statuses are RESOLVED**

Run:
```bash
grep -n "OPEN\|RESOLVED" audits/AUD-2026-04-25-002.md
```
Expected: no `OPEN` matches; three `RESOLVED` matches (one per finding status field).

---

### Task 5: Commit all changes

**Files:** all 4 modified files

- [ ] **Step 1: Stage all 4 files**

```bash
git add user-guide.md template/scheduled-tasks/ops/migrate.md template/scheduled-tasks/ops/audit.md audits/AUD-2026-04-25-002.md
```

- [ ] **Step 2: Verify staged files**

Run:
```bash
git diff --cached --name-only
```
Expected:
```
audits/AUD-2026-04-25-002.md
template/scheduled-tasks/ops/audit.md
template/scheduled-tasks/ops/migrate.md
user-guide.md
```

- [ ] **Step 3: Commit**

```bash
git commit -m "$(cat <<'EOF'
fix: resolve all 3 findings from audit AUD-2026-04-25-002

- user-guide.md: remove duplicate token table row (finding 012)
- migrate.md: remove ingest.md from UNTOUCHED block (finding 013)
- audit.md: add claude-code-enhanced glob to estimate command (finding 014)
- AUD-2026-04-25-002.md: mark all findings RESOLVED, risk CLEAN

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 4: Verify commit**

Run:
```bash
git log --oneline -3
```
Expected: new commit at top referencing AUD-2026-04-25-002.
