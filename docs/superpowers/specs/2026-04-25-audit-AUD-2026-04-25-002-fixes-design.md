# Design: Fix Audit Report AUD-2026-04-25-002 Findings

**Date:** 2026-04-25
**Audit Report:** `audits/AUD-2026-04-25-002.md`
**Scope:** 3 findings (0 CRITICAL, 2 WARNING, 1 STYLE)
**Approach:** Read-all-then-batch — read all 3 files upfront with opportunistic sweep, apply all edits, single commit at the end.

---

## Root Cause

The AUD-001 fix cycle resolved all 11 prior findings but introduced one new exposure: reconciling the two token-table rows to the same value (finding 002) exposed that both rows described the same scenario — the duplication was hidden while the values differed. Two other issues are carry-forward artifacts: `ingest.md` was left in the UNTOUCHED block of `migrate.md` after `conventions.md` was removed from it (finding 010 fix was incomplete), and the `audit.md` estimate command was never updated when the `claude-code-enhanced` skill was added.

---

## Section 1 — `user-guide.md` (Finding 012)

| Finding | Location | Change |
|---|---|---|
| 012 WARNING | line ~230 | Remove duplicate row `\| !! ready with full memory \| ~1,830 \|`. Keep row at line ~222 (`Cold start with !! ready (full memory) | ~1,830`) — it is the canonical, more complete label. |

**Sweep result:** File is otherwise clean. Line 14's token figure was reconciled in the AUD-001 cycle. The slash-command count on line ~111 (3 commands) is intentionally correct — `/sync` is developer-only per the AUD-009 fix. No additional changes.

---

## Section 2 — `template/scheduled-tasks/ops/migrate.md` (Finding 013)

| Finding | Location | Change |
|---|---|---|
| 013 WARNING | line ~59 | Remove `ingest.md (beyond advisory update)` from the UNTOUCHED block entirely. |

Corrected UNTOUCHED block after fix:
```
Files UNTOUCHED:
  wiki/  (all pages, index, log, hot preserved)
  memory.md, raw/, drafts/
  sqlite-query skill (if installed)
```

**Sweep result:** File is otherwise clean. Step 5 slash-command mention (`/wrap`, `/ready`, `/migrate`) is consistent with user-guide.md. No additional changes.

---

## Section 3 — `template/scheduled-tasks/ops/audit.md` (Finding 014)

| Finding | Location | Change |
|---|---|---|
| 014 STYLE | line ~93 Notes | Append `blueprint/skills/claude-code-enhanced/*.md` to the example estimate command. |

Updated estimate command:
```
python scripts/estimate_tokens.py blueprint/README.md blueprint/setup-guide.md blueprint/user-guide.md blueprint/troubleshooting.md blueprint/template/CLAUDE.md blueprint/template/scheduled-tasks/refresh-hot.md blueprint/template/scheduled-tasks/ops/*.md blueprint/skills/sqlite-query/*.md blueprint/skills/claude-code-enhanced/*.md
```

**Sweep result:** File is otherwise clean. The `!! audit all` scope block (lines 13–24) already correctly covers all skill subdirectories — only the example Notes command was lagging. No additional changes.

---

## Section 4 — Audit Report Status Update

After all 3 file edits: update `audits/AUD-2026-04-25-002.md`:
- Set **Status** of findings 012, 013, 014 from `OPEN` → `RESOLVED`
- Check all 3 action-item boxes (`- [ ]` → `- [x]`)
- Update Executive Summary Overall Risk from `HIGH` → `CLEAN` (zero open findings)

---

## Commit

One commit covering all 4 files, message referencing `AUD-2026-04-25-002` and noting all 3 findings resolved.

---

## Files Touched

| File | Findings | Type |
|---|---|---|
| `user-guide.md` | 012 | Content |
| `template/scheduled-tasks/ops/migrate.md` | 013 | Structural |
| `template/scheduled-tasks/ops/audit.md` | 014 | Content |
| `audits/AUD-2026-04-25-002.md` | all | Status update |
