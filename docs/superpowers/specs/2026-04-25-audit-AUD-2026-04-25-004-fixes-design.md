# Audit AUD-2026-04-25-004 Fixes + documents/ Merge — Design

**Date:** 2026-04-25
**Status:** Approved

---

## 1. Scope

Two independent tasks delivered in a single session:

1. **Audit fixes** — resolve all 8 open findings from AUD-2026-04-25-004 (findings 015–022)
2. **Folder merge** — consolidate `documents/` into `docs/`, then delete `documents/`

---

## 2. Audit Fixes

All 8 findings stem from the v2.3 Query Routing Rule redesign (wiki-first waterfall) not being cascaded to every affected file. The fix is a strict text substitution per finding — no logic changes, no new features.

### Root cause (shared)
`template/CLAUDE.md` and `migrate.md` were updated for v2.3 but `README.md`, `user-guide.md`, `conventions.md`, `query-layer.md`, `audit.md`, and `reference.md` were not.

### Changes

| Finding | Severity | File | Change |
|---|---|---|---|
| AUD-015 | CRITICAL | `README.md:74` | Replace waterfall bullet: "answers from training first, then wiki, then web search" → "wiki first, then web search, then training knowledge (fallback only, with confidence score)" |
| AUD-016 | CRITICAL | `user-guide.md:51–55` | Replace 3-step waterfall list + filing-offer sentence with v2.3 order (wiki → web → training) and correct step references |
| AUD-017 | WARNING | `user-guide.md:~187` | Fix compliance example: replace "Step 1 on a wiki question" with "Step 3 (training knowledge) on a question that should be in your wiki" |
| AUD-018 | WARNING | `template/scheduled-tasks/ops/conventions.md:125` | Update "Filing Answers" heading and first sentence: Step 2/3 → Step 1/2; wiki/web labels corrected |
| AUD-019 | WARNING | `template/CLAUDE.md` Step 2 bullet list | Add third branch: "web directly answered AND Step 1 returned partial wiki content → ingest web result; answer citing both" |
| AUD-020 | WARNING | `skills/sqlite-query/query-layer.md:5` | Update self-description: "(Step 2)" → "(Step 1 (wiki))" |
| AUD-021 | STYLE | `template/scheduled-tasks/ops/audit.md:14–24` | Add `blueprint/CHANGELOG.md` to `!! audit all` scope between `troubleshooting.md` and `LICENSE` |
| AUD-022 | STYLE | `template/scheduled-tasks/ops/reference.md` | Add `scripts/` subtree (6 files) under `blueprint/template/` in the directory tree |

**After all fixes:** Mark all 8 findings OPEN → RESOLVED in `audits/AUD-2026-04-25-004.md`.

---

## 3. Folder Merge

### Source → Destination

| Source | Destination |
|---|---|
| `documents/plans/2026-04-25-cold-start-optimization.md` | `docs/superpowers/plans/` |
| `documents/specifications/2026-04-25-cold-start-optimization-design.md` | `docs/superpowers/specs/` |
| `documents/audit-report-template.md` | `docs/` (root) |
| `documents/` (folder) | Delete after move |

No file renaming. No content edits. Git-move to preserve history.

---

## 4. Commit Plan

Two commits in order:

1. `fix: apply AUD-2026-04-25-004 findings (015–022), mark all RESOLVED`
   - 7 content files + `audits/AUD-2026-04-25-004.md`

2. `refactor: merge documents/ into docs/, remove documents/`
   - 3 file moves + folder deletion

---

## 5. Out of Scope

- No changes to any other audit findings (prior cycles already RESOLVED)
- No refactoring beyond what the findings specify
- No changes to wiki content, scripts, or skills beyond the two targeted skill files
